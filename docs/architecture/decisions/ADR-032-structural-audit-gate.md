# ADR-032: Structural Audit Gate (Task 38.5)

**Status:** Accepted

## Context
Tasks 1–38 each produced a foundational component or framework — trading-domain frameworks, infrastructure frameworks like Exchange Adapters, and eventually the composition root itself. Task 38 specifically declares and wires **24** of those completed frameworks (`app/wiring.py:COMPONENT_REGISTRARS`) into a disposable dry-run container that proves the declared DI graph for those 24 wires. 38 tasks and 24 wired frameworks are not the same count, and this ADR does not conflate them: several Tasks 1–38 produced infrastructure (`core/`, `config/`, `events/`, `trading/`) or a framework not among the 24 wired ones (`exchange_adapters/`, Task 17 — see H-2 below), not one of the 24 `COMPONENT_REGISTRARS` entries. No task to date had re-examined the system *as a whole*, from real committed source rather than prior claims, for structural cracks that only become visible once every framework is considered together — cross-framework coupling no single framework's own review would catch, a safety boundary asserted in one task's docstring but never checked against a sibling's real behavior, or a trading-safety control the roadmap assumes exists but that no task actually built. `docs/prompts/task-38.5.md` specified a code-read-only audit to close that gap and, critically, to establish a gate: Task 39 should not begin work on top of an unverified foundation.

## Decision
Adopt a standing rule: **any open Critical-severity finding from a Task 38.5-class structural audit blocks the next dependent task from starting**, until each such finding is either resolved by a dedicated follow-up task or explicitly re-classified with a recorded justification. This gate is independent of what any particular audit run finds — it was decided as an architectural policy in `docs/prompts/task-38.5.md` before the audit ran, and this ADR records that policy as accepted regardless of outcome.

**Amendment, this closeout pass:** the policy above was written assuming the audit could conclusively determine whether a Critical finding exists. Five successive passes (v1–v5) of this audit each found something the previous pass's method could not see, which is itself evidence the trace has not converged on exhaustive. The policy is therefore amended to make explicit what it already implied: **if the audit cannot conclusively establish the absence of a Critical-severity path — as opposed to having discovered zero within what it walked — the gate is HOLD, not ALLOWED**, until either the assurance gap is closed by a stronger method or a deliberate, recorded decision accepts the residual risk. This is a distinct disposition from BLOCKED (which means a Critical finding *was* discovered); HOLD means the question was not conclusively answered either way.

**This run's outcome (honest closeout pass, 2026-08-20 — a document-only correction of language and gate disposition; the coverage evidence itself, gathered by the v5 patch pass, is unchanged and restated below):** the Task 38.5 audit (`docs/audits/task-38.5-structural-audit.md`, `docs/audits/task-38.5-risk-register.md`, `docs/audits/task-38.5-test-gaps.md`) records **0 Critical discovered, 2 High, 7 Medium, 9 Low** findings (v4 was 0/2/6/9; this pass adds **M-7**, naming the audit-assurance gap itself as a finding — no other count changed). **Gate outcome: INDETERMINATE / HOLD.** No forbidden trade, inference, network, DB, Redis, file, thread, or process path was found reachable from Task 38's own documented entrypoints (`app/main.py:main`, `app/bootstrap.py:run_dry_run_bootstrap`) *within the scope this audit's method actually covers* — confirmed by a recursive trace using 24 independent fresh containers, cross-checked by a static AST call inventory that walks `core.container.ServiceContainer`'s and `core.registry.ServiceRegistry`'s own methods, `app.planner.plan`, and `app.preflight.validate_service_keys`/`.run` as real, source-inspected targets, and resolves every `super().__init__()` call to its actual MRO base. **183** distinct real classes were reached and classified with **0 left unresolved** *by the classifier's own bookkeeping*; **155** unique calls were classified into six buckets with **0 unresolved** *by the same bookkeeping*; a module-state inventory classified 516 candidates with 0 unexplained *within its own scan's scope*; a runtime defence check patched a broad (not exhaustive — see M-7) I/O/lifecycle surface and `run_dry_run_bootstrap` still returned `SUCCESS` at `24/24`. **This is high-confidence coverage evidence, not a completeness proof** — see M-7 in the risk register for the four specific methodological gaps (`__post_init__` call-graph coverage; `os.open`/DB/Redis runtime-denial coverage; module-global mutation from a plain function; the residual text/name allowlist underlying "resolved"). **Task 39 must not begin on the strength of this audit alone; Task 38.6 — a source-controlled, reusable audit harness closing the four M-7 gaps — is the required follow-up before the gate is re-evaluated.** This is explicitly not the same thing as having found a Critical vulnerability: no such finding exists in this audit's record, at any severity above Medium.

The two High findings are real and are not waived by this decision: `app/wiring.py:3` and `docs/prompts/task-38.md:21`'s claim that "every prior framework is forbidden from importing another framework directly" is false for 14 of the 24 wired frameworks, plus `exchange_adapters` (which is not one of the 24 wired frameworks at all — a pre-existing architectural characteristic of the earlier trading-domain frameworks, not something Task 38 introduced or could have fixed within its own scope); and `COMPONENT_REGISTRARS`'s "all 24, every completed package" claim omits `exchange_adapters`, a fully-built Task 17 framework. Neither rises to Critical under the rubric below (neither opens a real I/O/trading/inference/credential-leak path), but both are recorded as open in the risk register and should be triaged by a near-term follow-up, not silently carried forward. **Both remain open and unimplemented after this pass**, exactly as before — this pass only corrects the audit's own language and gate disposition; it changed no code and does not resolve either finding.

**M-6 (Medium, open, not waived), refined in v5, unchanged by this pass:** `LoggerFactory.get_logger()` is called, guarded by `if logger else None`, at 46 distinct `__init__` sites (every `Manager` and `Engine` across the 24 wired frameworks). A default-configured `LoggerFactory` would attach a console handler (not a file) if reached — real file I/O (`os.makedirs`, `RotatingFileHandler`) additionally requires an explicit `file_path` configuration nothing in the current wiring sets. It is unreachable today, within verified scope, only because no registrar wires `LoggerFactory` into the dry-run graph — an application-level fact, not a structural guarantee a future registrar addition couldn't quietly break. See the risk register for full evidence and recommendation; not implemented here.

**M-7 (Medium, open, new in this pass):** the audit-assurance gap itself. Five passes each found a real gap in the previous pass's method; four specific, currently-open gaps remain (named above and in full in the risk register). This finding is the reason the gate is HOLD rather than ALLOWED. See the risk register for full evidence and the Task 38.6 recommendation; not implemented here.

**This run's outcome:** Task 38.6 harness build-and-run, 2026-08-20, committed at `8fd66ca`. The required harness now exists, is committed, was run against that commit, and the gate is re-evaluated — still HOLD, on a substantially narrower basis. `audit_harness/` (source-controlled, tested: `pytest tests/audit_harness/` → 16/16 pass) implements all nine Harness Requirements from `docs/prompts/task-38.6.md`. Its own five negative controls (Requirement 7) were run first and all five were detected (`self_test_failed=false`) before any other part of its result was trusted (implementation item 5).

The real run against commit `8fd66ca` found: 205 nodes (2 unresolved), 1956 calls (68 unresolved), 523 module-state candidates (0 unexplained), and a paper-only runtime-denial check that returned `SUCCESS` at `24/24` with no forbidden call observed. Every one of the 68 unresolved calls and both unresolved nodes is individually categorized in `docs/audits/task-38.6-assurance-report.md` §4 — third-party (`pydantic-settings`/`python-dotenv`) internals outside the exact-identity policy's deliberately stdlib-only scope (not described as safe), deep stdlib `logging` internals reached only through the already-known-unreached M-6 chain, local planner/preflight computation, `ServiceContainer._build`'s structurally-generic `cls(**kwargs)`, one `inspect.getsource` lambda-truncation quirk, and one real narrow typing gap (**L-10**, new) in `strategies/__init__.py`.

No forbidden identity was established among them; unresolved behavior remains safety-unknown, not proven benign, and that is exactly why this stays **HOLD**. Per the Gate Rule (`docs/prompts/task-38.6.md`), a nonzero unresolved count still keeps this ADR at HOLD — the harness closes the *open-ended* uncertainty M-7 named (replacing five rounds of "what did we miss this time" with one committed, re-runnable, self-testing tool) without yet closing the *bounded* residual that tool itself reports. Machine-readable result: `docs/audits/task-38.6-result.json`, hash `3a48d00cc626c1980b41496ea5c4e3e41e996b2327f6fa044f457f23ac019b82`.

**Task 38.7 Phase 0 — prospective `EXACT_IDENTITY_POLICY` authorizations, 2026-08-22.** Per `docs/prompts/task-38.7.md`'s Gate Outcome Requirements and Two-Phase Provenance (a human-reviewer authorization must be recorded here *before* any code implementing the corresponding table entry is written, never retroactively), the following two entries are each individually authorized, prospectively, ahead of their Phase A implementation:

1. **`builtins.str.strip`** (module `builtins`, qualname `str.strip` — the CPython `method_descriptor` reached via `str.strip`, resolved through `audit_harness.identity.module_and_qualname`'s `__objclass__` fallback path). **Irreducibility:** C-implemented; `inspect.getsource` raises `TypeError` (no retrievable `.py` source exists to walk); no general mechanism in `audit_harness/` — source-availability walking, dataclass/NamedTuple structural detection, or any other Requirement 1–5 technique — can classify a C-boundary `method_descriptor` past `unresolved`. **Operational boundary:** a pure string operation (returns a new string with leading/trailing whitespace or given characters removed), no I/O, no mutation of the receiver (immutable `str`), deterministic — the same operational boundary already accepted for the existing `builtins.str.lower`/`builtins.str.rsplit` entries. **Accepting reviewer:** Aniket Bhalerao — project owner/reviewer. This authorization precedes, and is separate from, its Phase A implementation.
2. **`builtins.list.append`** (module `builtins`, qualname `list.append` — the CPython `method_descriptor` reached via `list.append`, resolved through the same `__objclass__` fallback path). **Irreducibility:** C-implemented; `inspect.getsource` raises `TypeError`; no general mechanism resolves it — the walker's subscript-value-type-inference and local-variable-type-inference mechanisms correctly identify a receiver's type as `list`, but the `append` method itself remains a C boundary requiring its own policy entry, exactly as `builtins.set.add`/`builtins.set.discard`/`builtins.dict.pop`/`builtins.dict.clear` already do for their respective containers. **Operational boundary:** a pure in-process list mutation (appends one element in place, returns `None`), no I/O, deterministic. **Accepting reviewer:** Aniket Bhalerao — project owner/reviewer. This authorization precedes, and is separate from, its Phase A implementation.

**`builtins.str.upper` is explicitly NOT authorized by this decision** — an isolated in-memory simulation found zero real call-records anywhere in the live trace currently resolve to it; the only tests referencing it are synthetic fixtures, which are being updated to use the already-authorized `builtins.str.lower` instead rather than authorizing an identity with no real-residual benefit. **`builtins.str.upper` remains absent from `EXACT_IDENTITY_POLICY` and any call reaching it remains `unresolved`.** `builtins.str.lower` is unaffected by this decision — it was already an existing, already-implemented entry predating Task 38.7, and neither its key nor its rationale is changed here.

This paragraph records the Phase 0 authorization only. It implements nothing itself; the corresponding `EXACT_IDENTITY_POLICY` table entries, their individual code-level rationale strings, and their regression tests are Phase A work, committed separately per Two-Phase Provenance.

**This run's outcome (Task 38.7 Phase A/B, 2026-08-22 — gate re-evaluated per Gate Outcome Requirements, Layers 1–3):** Task 38.7's Phase A source/tests commit (`dcb1ebc6b7fd34e89979fd9adf8ed0d57bc8ac58`) implements Remediation Requirements 1–6 (source-availability/termination, transitive local-variable inference, subscript/narrowing inference, call-site specialization, lambda AST location, the one-line `strategies/__init__.py` annotation fix), the `mappingproxy` qualname-correction disposition, the `calls_unresolved_detail_multiplicity` schema addition (`38.6.0` → `38.7.0`), and the two ADR-032 Phase 0-authorized `EXACT_IDENTITY_POLICY` entries above. Phase B's fresh re-run against that commit (`docs/audits/task-38.7-result.json`, hash `bdbe9cffd1b770140af150e2d66a6524e078f17a817f99bb5d1ec771a7731c0b`; narrative: `docs/audits/task-38.7-assurance-report.md`) found: **258 nodes (19 unresolved)**, **6783 calls (1032 unresolved)**, **523 module-state candidates (0 unexplained)**, and a paper-only runtime-denial check returning `SUCCESS` at `24/24` with no forbidden call observed. `unimportable_nodes=[]` — the `builtins.mappingproxy: import failed` entry is fully closed.

**Layer 1 (the coded predicate): fails.** `nodes_unresolved=19` and `calls_unresolved=1032` are both nonzero — the two conditions Task 38.6's `2`/`68` baseline also failed on, now measured against a substantially wider, correctly-broadened walked universe (`calls_total` grew from 1956 to 6783, `nodes_total` from 205 to 258, precisely because Category A's requirement to walk third-party source the same way project-owned code is walked was implemented — not a regression; see the assurance report §4 for the full accounting). Both nodes the `8fd66ca` baseline named (`config.settings.Settings`, `pydantic_settings.main.BaseSettings`) are confirmed resolved; the current 19 unresolved nodes are newly-discovered stdlib C-level types reached only through that same broadened third-party walk, none forbidden. The two ADR-032-authorized policy entries closed 20 raw calls within this task's own work (`1052 → 1032`); a further, larger residual remains (§4.2 of the assurance report — dominated by `builtins.str.join` (191), `core.interfaces.Registration.provider` (183, confirmed genuinely dynamic), `builtins.mappingproxy.items` (110), and 326 calls with no resolved identity at all), none of it authorized for a Phase 0 policy entry this pass.

**Layer 2 (`unimportable_nodes`): closed.** Resolved via the preferred path — `types.MappingProxyType` qualname correction — confirmed empty on the Phase B re-run.

**Layer 3 (H-2 and other independent findings): H-2 remains open, unremediated by this task, as required by H-2 Reconciliation** (`missing_from_component_registrars=["exchange_adapters"]`, re-confirmed unchanged on this run). H-1 likewise remains open (Task 38.7's scope does not touch it). **M-8 (added post-evidence, 2026-08-23 — see below) is an additional open Layer-3 assurance finding.** **L-10 is now closed** (`docs/audits/task-38.5-risk-register.md`'s L-10 entry, citing this Phase A fix and this Phase B evidence) — the one finding this task's own scope was authorized to remediate.

**Gate outcome: HOLD, unchanged.** Layer 1 alone is sufficient to keep this ADR at HOLD regardless of Layers 2–3; Layer 3's H-2 (and, separately, M-8) would independently keep it at HOLD even were Layer 1 to reach zero. **Task 39 remains blocked.** This is not a claim that `ADR-032` is ALLOWED, that Task 38.7 is complete, or that Task 39 is unblocked — a future task must close the node/call families the assurance report names (or record further individually-authorized Phase 0 entries for identities found genuinely irreducible) and separately, and explicitly, resolve H-2 and H-1, before the gate can move.

**Post-evidence addendum, 2026-08-23 — M-8 (implicit protocol-dispatch discovery gap).** A follow-up diagnostic pass, conducted after Task 38.7's Phase B evidence above was recorded, established that `StaticWalker`'s discovery mechanism (literal `ast.Call` nodes in walked source) has no corresponding mechanism for execution the interpreter dispatches implicitly — iteration, context managers, comparisons, truth testing, subscription, membership, hashing, string formatting, arithmetic operators, and property/descriptor access. Reproduced directly and repeatedly without ever executing the reproduction: non-executed, git-untracked scratch fixtures embedding a call to a real forbidden identity inside each relevant dunder method, triggered only through the ordinary syntax invoking that protocol, produced no call record at all for the embedded call in every family tested. This is recorded as `docs/audits/task-38.5-risk-register.md`'s **M-8** (Medium, Open) — see that entry for the full evidence, the complementary (execution-time-only) role of the runtime-denial layer, and the explicit statement that no currently reachable forbidden production path is claimed. **This addendum changes no Layer 1 counter, does not close H-1 or H-2, does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review, and does not modify `EXACT_IDENTITY_POLICY`.** Machine-readable result cited above is unchanged: `docs/audits/task-38.7-result.json`, hash `bdbe9cffd1b770140af150e2d66a6524e078f17a817f99bb5d1ec771a7731c0b`.

**Task 38.8 Phase 0 — architecture and safety-boundary decision, 2026-08-24.** Per `docs/prompts/task-38.8.md` §5/§9, Option **D** (a justified hybrid) is selected, scoped to exactly **2 of the 11** protocol families §2 names: **context managers** (`__enter__`/`__exit__`, including async equivalents) and **property/descriptor access** (`__get__`/`__set__`/`__delete__`). The remaining **exactly 9** families are unsupported under this decision, each reported in `unsupported_protocol_families` and each requiring its own later, separately authorized Phase 0 analysis before it may be added to this scope: **(1)** iteration/comprehensions, **(2)** unpacking, **(3)** await/async iteration, **(4)** equality/ordering/arithmetic operators including reflected forms, **(5)** truth testing, **(6)** membership, **(7)** subscription/assignment/deletion, **(8)** hashing, **(9)** formatting including `__str__`/`__repr__` fallback. (Full, unselected Option A would mechanize all **11**; this decision selects neither Option A's 11 nor Option C's 0, but Option D's named 2.)

**Syntax sites vs. dispatch events (context managers).** One `ast.With`/`ast.AsyncWith` item is one syntax site and produces **two** protocol-dispatch events — `__enter__` and `__exit__` — each identified by `(module, qualname, line, column, family, protocol_method)`. No context-manager syntax site is ever claimed to have one target; `syntax_sites_total` and `dispatch_candidates_total` are reported as two distinct figures, reconciled only via `dispatch_candidates_total == resolved_dispatches + unresolved_dispatches`, with `explicit_path_duplicates` as a subset annotation, never an addend.

**Syntax sites vs. dispatch events (descriptors), and the full attribute-residual set.** One `ast.Attribute` node is one syntax site; its AST context determines the operation implicated — `Load`→`__get__`, `Store`→`__set__`, `Del`→`__delete__` — so each site contributes at most one dispatch candidate for the context-appropriate operation. An unresolved receiver emits one `unresolved` candidate for that operation; a resolved receiver whose attribute is an ordinary, non-descriptor attribute contributes a `resolved_non_descriptor_exclusion`, counted separately and excluded from the `resolved_dispatches`/`unresolved_dispatches` partition entirely. **This exclusion proves only that no descriptor dispatch occurs for the context-appropriate operation at that site — it does not claim, and must never be read as claiming, that no implicit execution occurs there at all.** A class overriding the base attribute-access machinery remains a named, unsupported residual across all four relevant contexts: **`Load`** may invoke a custom `__getattribute__` or `__getattr__`; **`Store`** may invoke a custom `__setattr__`; **`Del`** may invoke a custom `__delattr__`. All four names are recorded as unsupported residual.

**Terminology — false negatives vs. observed uncertainty.** A descriptor or context-manager site the mechanism enumerates is reported as one of two distinct kinds of observed uncertainty, never a false negative, when it cannot be classified resolved-safe or resolved-forbidden: **(a)** the receiver cannot be resolved to a concrete type at all; or **(b)** the receiver *is* resolved and a concrete dunder target *is* found, but that target is C-implemented or otherwise source-unavailable-by-construction and is classified `unresolved` under the walker's existing source-availability rule. A **false negative** is reserved exclusively for a site or path the mechanism never enumerates at all.

**Ten-criteria evaluation, with concrete reasoning recorded here:**
1. **Soundness** — a resolution is accepted only when the receiver's static type and the context-appropriate dunder target are both soundly established by the walker's existing identity-first discipline (`_resolve_target`/`classify_callable`). AST syntax (the `ast.With`/`ast.AsyncWith`/`ast.Attribute` shape and its context) **selects which protocol-method candidate is under consideration at a site — it identifies the candidate, never verifies it.** Only the resolved, live callable identity that candidate actually points to receives a classification verdict. AST shape is never asserted to play no role in soundness — it is exactly what makes a site a candidate in the first place; its role is bounded to candidate-selection, never substituted for the identity-based verdict itself.
2. **Polymorphism/duck typing** — an unresolvable receiver (case (a) above) produces an explicit `unresolved` dispatch record, never a default-safe verdict; a `Protocol`-typed or untyped-parameter receiver is treated identically to any other unresolvable receiver under the walker's existing type-inference limits.
3. **False negatives, precisely qualified** — confined, within the scope this decision actually walks, to genuinely **accepted** `StaticWalker.walk()` states (post visit-key dedup, post budget, post source-availability) and to the **two selected AST shapes**. This is not a claim of complete or exhaustive coverage beyond that narrow scope: an unreachable-source/graph-reachability gap (a callable the walker never visits at all, `docs/prompts/task-38.8.md` §1's third category — unchanged, out of this decision's scope), a receiver whose declared type diverges from its actual runtime type (criterion 4), and any instance of the **9 unsupported families** all remain real, named residual risks this decision does not close and must never be described as covered.
4. **False positives/manufactured confidence** — bounded by the same declared-vs-runtime-type risk the walker's existing parameter-annotation substitution already carries; this decision introduces **no new category of inference risk, but the existing risk is extended to the newly enumerated context-manager and descriptor sites.**
5. **Deterministic termination** — any recursion into a resolved `__enter__`/`__exit__`/`__get__`/`__set__`/`__delete__` target is gated by the walker's existing `(callable identity, specialization)` visited-state dedup and existing `max_total_walk_steps` budget; no separate recursion or budget mechanism is introduced.
6. **Specialization-state growth** — not estimated here. Phase B's real re-run against the committed Phase A.1 mechanism is what measures this.
7. **CPython-version coupling** — materially lower and more stable than the rejected bytecode-based Option B, but not zero: the AST shapes this decision inspects are stable across recent CPython minor versions but are not immune to language-level change, and this decision's soundness is bounded specifically by this repository's `==3.12.*` pin (`pyproject.toml:6`), never asserted as version-independent.
8. **Maintenance burden** — bounded to two families' worth of new AST-shape handling in `audit_harness/`, structurally smaller in scope than mechanizing the full 11 under Option A (a scope comparison, not a magnitude estimate).
9. **Implementation size** — not estimated here. Phase A.1's committed implementation is what establishes this.
10. **Fail-closed gate compatibility** — the additive schema fields this decision requires (`docs/prompts/task-38.8.md` §8), and any new `all_clear`-contributing term they introduce into `audit_harness/report.py`, **are themselves new plumbing** — never assumed safe merely by routing toward existing machinery. Missing implicit-dispatch evidence (fields absent or unpopulated), malformed evidence (a schema violation), work-budget exhaustion during context-manager/descriptor resolution, and any unresolved implicit-dispatch record of either kind (a) or (b) above must each independently keep `ADR-032` at HOLD — none may be defaulted, ignored, or treated as passing, and the new `all_clear` term (if one is introduced) must be proven, by a dedicated regression test, to require all four clear before Phase A.1's mechanism may be trusted.

**The 9 deferred families, stated precisely:** `await`/async iteration requires its own future Phase 0 analysis of `__await__`'s **multi-stage iterator execution** (a coroutine's `__await__` yields across a generator-based state machine, not a single call-and-return); subscription requires its own analysis of **instance/class/metaclass dispatch distinctions** (`obj[k]` vs. class-level `SomeGeneric[T]` via `__class_getitem__` vs. a metaclass's own `__getitem__` are three different dispatch paths); hashing requires its own analysis of `__hash__`, which **may be triggered by set/dict-key insertion — insertion is the direct triggering mechanism, not a fallback**; formatting (`__format__`, with its own documented `__str__`/`__repr__` fallback) **remains explicitly unsupported**, with no partial coverage claimed. None of the 9 is a straightforward follow-on addition to this scope; each requires its own future, separately authorized Phase 0 record before any implementation work on it begins.

**Phase discipline.** Phase A.0 (once authorized) produces only committed characterization fixtures/tests for **all 11** families — no workload, census, or site-count claim of any kind. Phase A.1 implements the mechanism and post-remediation tests for the **2** mechanized families, plus the `audit_harness.identity` module-docstring boundary statement and `unsupported_protocol_families`/negative-control schema support for the **9** residual families. Phase B re-runs the full harness and produces the new Task 38.8 assurance report, including the bounded-scope disclosure for the same **9** residual families, grounded in Phase A.0/A.1's committed behavior. Phase C updates M-8 and this ADR from that committed Phase B evidence only.

**This decision changes nothing else.** It records only the Phase 0 architecture selection above and does not itself implement any code, run the harness, or change any Layer 1 counter. **M-8 remains Open** (this 2-of-11-family, still-partial scope could support `Open, narrowed` at most, once real Phase C evidence exists — never `Closed`, since 9 families remain entirely unmechanized). **H-1 and H-2 remain Open**, independently. `builtins.str.join`'s separately pending Phase 0 review remains **paused and unrecorded** — neither authorized nor rejected by this decision. `EXACT_IDENTITY_POLICY` is **not modified** by this decision in any way. **Gate outcome: unchanged, HOLD.** *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.8 Phase B/C — evidence and M-8 disposition update, 2026-08-31.** Phase A.1 (mechanism and post-remediation tests for the 2 Option-D-selected families, committed `b338484f8e6e5cd1af1f69260cdb2b044b0db0c5`) and Phase B (full harness re-run, committed `fa66cea8282ac56dc742289cd03272b18b69318a`; machine-readable result `docs/audits/task-38.8-result.json`, schema `38.8.0`, hash `7f5ffdf883339fa3e363db8d4a394ba49bb8b0e2416a4e47d7b9790395a7ae53`; narrative `docs/audits/task-38.8-assurance-report.md`) are both committed. The real re-run found: `nodes_total=261` (`nodes_unresolved=22`), `calls_total=6842` (`calls_unresolved=1071`, explicit-call-only per §8), `negative_controls` 7/7 detected, paper-only runtime denial `SUCCESS` at `24/24` with no forbidden call observed, and two independent runs against the same commit byte-identical (determinism confirmed). The new `implicit_dispatch` section reports `syntax_sites_total=10064`, `dispatch_candidates_total=6724` = `resolved_dispatches` (**119**) + `unresolved_dispatches` (**6605**) — a genuine partition — plus `resolved_non_descriptor_exclusion=3642` (excluded from that partition, not summed into it) and `explicit_path_duplicates=0` (an annotation, never a fifth addend). Per dispatch method: `__enter__` 0/301 resolved, `__exit__` 0/301, `__aenter__`/`__aexit__` 0 candidates (no async context managers reached on this run), `__get__` 119/6104, `__set__` 0/18, `__delete__` 0 candidates. `unsupported_protocol_families` reports the same 9 families named at Phase 0, verbatim.

**Gate outcome: HOLD, unchanged.** `nodes_unresolved=22` and `calls_unresolved=1071` are each independently sufficient to keep Layer 1 failing; `implicit_dispatch.unresolved_dispatches=6605` is a further, independent nonzero count this decision's own criterion 10 committed to treating as new plumbing that must itself keep the gate at HOLD, and it does. H-2 remains open (Layer 3), independently. **Task 39 remains blocked.** This Phase C update completes Task 38.8's own defined Phase 0/A.0/A.1/B/C scope once committed — a distinct claim from gate clearance: no claim is made that `ADR-032` is ALLOWED, that M-8 is `Closed`, or that Task 39 is unblocked.

**M-8 disposition: `Open` → `Open, narrowed`** (`docs/audits/task-38.5-risk-register.md`, updated in the same commit as this paragraph). Per this decision's own gate/policy constraint — a hybrid architecture's per-site evidence may support `Closed` only for a family it fully covers with **zero** unresolved sites on the same run — neither mechanized family reaches zero here (`__enter__`/`__exit__` are 0-of-301 resolved each; `__get__` is 119-of-6104; `__set__` is 0-of-18), so this evidence supports `Open, narrowed`, never `Closed`, for any family, and the 9 unmechanized families remain `Open` outright pending their own future, separately authorized Phase 0 decisions. This narrows the finding from an undifferentiated 11-family discovery gap to: two families now covered by a real, tested, per-site mechanism with a large but honestly-reported unresolved residual, and nine families still entirely undiscovered by construction — never described, here or anywhere, as a blanket allowlist or a trust rule for any dunder method or protocol name.

This update changes no Layer 1 counter beyond what Phase A.1/B's own broadened `explicit_calls` accounting already reflects, does not close H-1 or H-2, does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review (still paused and unrecorded), and does not modify `EXACT_IDENTITY_POLICY`. A future, separately scoped and authorized task would be required to drive `implicit_dispatch.unresolved_dispatches` toward zero within the 2 mechanized families, or to run this task's own Phase 0 process again for any of the 9 remaining families — neither is undertaken by this Phase C update. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.9A — H-2 remediation, implementation and evidence, 2026-08-31.** Implementation (`app/wiring.py` wires `exchange_adapters` as the 25th `COMPONENT_REGISTRARS` entry; `_COMPONENT_SERVICE`/`SAFE_SERVICE_KEYS` gain a corresponding `"exchange_adapters.manager"` → `DefaultExchangeManager` entry) committed `b8ebb23531350d30924e777dfdcbd295311f4f10`; Phase B evidence committed `384b5486e74a463b78be648590069e569becd95d` (`docs/audits/task-38.9a-result.json`, hash `a1a44b3b793c873a087db21a1e9e4af4e3be0995ba5350f8923a8b3008c6ebc6`; narrative `docs/audits/task-38.9a-assurance-report.md`, hash `0b8a8e83b051fabdde9d43e231c66a50547cbc119e9e8ebe616fcb1162c91424`). The real re-run found: `discovery.missing_from_component_registrars=[]` (`exchange_adapters` no longer missing); `roots_traced=25`; real dry-run composition `25/25` (`status=SUCCESS`); `runtime_denial_checks`: `bootstrap_status=SUCCESS`, `preflight_total/passed/failed=25/25/0`, `forbidden_call_observed=None`, `success=True`; the newly discovered `exchange_adapters.engine.DefaultExchangeEngine.start` lifecycle target is under the same defensive, rule-based runtime-denial coverage as every other framework's engine, without being invoked during the real dry-run; negative controls 7/7 detected; full project suite `937 passed`.

**H-2 disposition: `Open` → `Closed`** (`docs/audits/task-38.5-risk-register.md`, updated in the same commit as this paragraph). H-2's specific finding — `exchange_adapters` completed but absent from `COMPONENT_REGISTRARS`, contradicting the "prove the whole system wires" premise — is fully remediated by the evidence above: the framework registers, its own DI graph resolves cleanly and I/O-free, and the composed dry run now proves `exchange_adapters`' graph wires exactly as the other 24 frameworks' graphs already did. Unlike M-8 (`Open, narrowed` — see above), H-2 supports full closure because the remediation is not partial: `exchange_adapters` is now fully covered by the exact same mechanism as every other framework, with no remaining unresolved-by-construction scope specific to this finding.

**Gate outcome: HOLD, unchanged.** H-2's closure does not move the gate. Layer 1 alone remains independently sufficient to keep this ADR at HOLD: `nodes_unresolved=22` and `calls_unresolved=1092` (grown from Task 38.8's `1071` because `exchange_adapters` is now a 25th walked root, not because anything regressed) are both nonzero, and `implicit_dispatch.unresolved_dispatches=6888` (likewise grown from `6605` for the same reason) is a further, independent nonzero count. **H-1 remains open**, unremediated by this task. **M-8 remains `Open, narrowed`**, unchanged by this task. **Task 39 remains blocked.** This is not a claim that `ADR-032` is ALLOWED, that this task closes the overall audit, or that Task 39 is unblocked — H-2 is one of several independently tracked findings/blockers, and closing it does not close the others. Task 38.9A completes its own defined H-2 remediation scope once this Phase C update is committed; that completion is a distinct claim from gate clearance.

This update does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review (still paused and unrecorded), and does not modify `EXACT_IDENTITY_POLICY`. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.9B — H-1 remediation, implementation and evidence, 2026-08-31.** Phase A (`app/wiring.py` and `docs/prompts/task-38.md` docstrings corrected from the false blanket "no framework imports another framework directly" claim to the narrower, evidenced invariant the repository actually satisfies; new source-controlled regression guard `tests/test_h1_cross_framework_boundary.py`) committed `78b5332197e03777b689794bdd9cbbd4b60e6e77`; Phase B evidence committed `c628a9d5473da3377d7679de1a7fcd5ef687648b` (`docs/audits/task-38.9b-result.json`, schema `38.9b.1`, hash `633ab16e1c85a61c85ca232e53af68c03f1db3aceb087bac53066db5af964b6c`; narrative `docs/audits/task-38.9b-assurance-report.md`, hash `31cafda06e43e9c85507554de5c6eee4d0f000f5bacf2d11133b9f926ac25eaa`). Phase B independently regenerated the cross-framework import inventory against the committed Phase A SHA and confirmed: **203** raw cross-framework import records across **15** importing frameworks; import classification **125** domain/value + **31** Protocol/interface + **9** concrete + **38** TYPE_CHECKING-only = **203**; the sole reviewed concrete cross-framework runtime type is `trading.engine.TradingEngine`, with exactly **9** runtime importers, **0** direct concrete construction, **0** other runtime references beyond `resolver.has`/`resolver.resolve` (**9** + **9** = 18), and **0** direct `TradingEngine.start`/`.stop`/`.pause`/`.resume` calls; the targeted regression suite (**7 passed**) and full project suite (**944 passed**) both pass.

**H-1 disposition: `Open` → `Closed`** (`docs/audits/task-38.5-risk-register.md`, updated in the same commit as this paragraph). H-1's defect was the false blanket architectural claim itself, not evidence of an unsafe live-execution path requiring a migration of the 15 frameworks (14 originally named, plus `exchange_adapters`) that import cross-framework to a DI-only pattern. Phase A corrected the claim and installed a regression guard for the narrower invariant actually satisfied by the repository; Phase B independently regenerated the inventory and confirmed the corrected invariant holds: frameworks are **not** globally import-isolated — legitimate framework-to-framework imports of public domain/value types and `Protocol`/ABC interfaces are a real, evidenced, intentional pattern, not a violation; no framework directly constructs another framework's concrete class; the sole reviewed concrete cross-framework runtime type is `trading.engine.TradingEngine`, used only as an optional, `resolver.has`-gated DI lookup key by exactly nine frameworks; and those nine tracked consumers never directly invoke its lifecycle methods. **This does not claim** that all cross-framework dependencies are interfaces (125 of 203 records are domain/value-type imports, not Protocols), that frameworks are globally import-isolated, or that `TradingEngine` is a `Protocol` — it is a concrete class used exclusively as a DI-key exception, and is described as exactly that.

**Gate outcome: HOLD, unchanged.** H-1's closure does not move the gate. Layer 1 alone remains independently sufficient to keep this ADR at HOLD: `nodes_unresolved=22` and `calls_unresolved=1092` are both nonzero, and `implicit_dispatch.unresolved_dispatches=6888` is a further, independent nonzero count — all three figures unchanged since Task 38.9A's evidence, since Task 38.9B performed no audit-harness re-run (Phase A's changes were docstring/test-only and do not add a new call site the harness's own walked universe would count). **H-2 remains `Closed`** (Task 38.9A), unchanged by this task. **M-8 remains `Open, narrowed`**, unchanged by this task. **Task 39 remains blocked.** This is not a claim that `ADR-032` is ALLOWED, that this task closes the overall audit, or that Task 39 is unblocked — H-1 is one of several independently tracked findings/blockers, and closing it does not close the others. Task 38.9B completes its own defined H-1 remediation scope once this Phase C update is committed; that completion is a distinct claim from gate clearance.

This update does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review (still paused and unrecorded), and does not modify `EXACT_IDENTITY_POLICY`. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.10 Phase 0 — exact-identity authorization (9 identities) and M-8 taxonomy scope correction, 2026-09-02.** Per this ADR's Two-Phase Provenance (a human-reviewer authorization must be recorded here *before* any code implementing the corresponding table entry is written, never retroactively), the following **nine** exact callable identities are each individually authorized, prospectively, ahead of their Phase A implementation. Baseline of record for this authorization: `HEAD = origin/main = 57fac8d7bd9f4ea8d152441ee7ca4a93afa02d9b`, working tree carrying only the untracked `uv.lock`.

1. `_thread.RLock.__new__`
2. `builtins.AssertionError.__new__`
3. `builtins.AssertionError.__init__`
4. `builtins.AttributeError.__init__`
5. `builtins.NotImplementedError.__new__`
6. `builtins.NotImplementedError.__init__`
7. `builtins.OverflowError.__new__`
8. `builtins.OverflowError.__init__`
9. `functools.partial.__new__`

**Accepting reviewer: Aniket Bhalerao** — project owner/reviewer, 2026-09-02. This authorization precedes, and is separate from, its Phase A implementation.

**Scope of the authorization — exact callable slots only.** These nine keys authorize the named `__new__`/`__init__` **slot** identities and nothing else. They do **not** authorize the corresponding **type** objects (`_thread.RLock`, `builtins.AssertionError`, `builtins.AttributeError`, `builtins.NotImplementedError`, `builtins.OverflowError`, `functools.partial`) as explicit-call targets; every one of those type identities remains `unresolved`, is unaffected by this record, and would require its own separate Phase 0 authorization, since calling the type runs both slots plus whatever the type's own construction protocol dispatches.

**Evidence basis (Task 38.10 Phase 0.2, evidence-only, no repository changes).** Phase 0.1's earlier, larger proposal of 23 identities is **withdrawn and not carried forward**; it rested on the narrower "this callable itself performs no file/network/process operation" predicate, which Phase 0.2 established is insufficient. Phase 0.2 applied a strictly stronger predicate — *an identity qualifies only if invoking it executes no user-overridable protocol hook and dispatches to no caller-supplied callable, neither at construction time nor by deferring one into a site the audit cannot enumerate* — and recorded the following basis for these nine:

- **CPython 3.12.13 is the interpreter of record**, matching `pyproject.toml:6`'s `requires-python = "==3.12.*"` pin and the repository's own `.venv`. This authorization is bounded to that pin and is never asserted as version-independent; the unresolved-component inventory itself differs on other minor versions (31 components on 3.12.13 versus 28 on 3.14), which is precisely why the interpreter is named here.
- **Each slot was characterized separately** — `Type.__new__(...)` and `Type.__init__(...)` were invoked individually against a harmless marker/counter probe class, never inferred from a whole-type constructor call, because the policy keys are themselves per-slot.
- **No construction-time user-controlled protocol callback was observed** for any of the nine. The probe exposed `__iter__`, `__next__`, `__index__`, `__int__`, `__trunc__`, `__bytes__`, `__buffer__`, `__str__`, `__repr__`, `__len__`, `__getitem__`, `keys`, `__call__`, `__hash__` and `__eq__`; none fired on any of the nine slots, in either the one-argument or multi-argument forms tested.
- **No file, network, process, or thread-start behavior.** `_thread.RLock.__new__` in particular creates and starts no thread — `_thread._count()` measured `0 → 0` across fifty constructions — accepts no user-supplied callable, performs no I/O, and mutates nothing beyond the object it allocates. The exception slots allocate and store the `args` tuple (and, for `builtins.AttributeError.__init__`, the `name=`/`obj=` keywords) verbatim.
- **No arbitrary supplied callable is invoked at construction** by any of the nine.
- **`functools.partial.__new__` stores the callable but does not invoke it** — measured invocations of the supplied `func` at construction time were `0`, becoming `1` only when the resulting `partial` was later called. Its nested-partial flattening reads C struct fields rather than Python attributes, verified against a `partial` subclass whose `func` is an overriding property: the property never ran and flattening still occurred, so no attribute-dispatch boundary is accepted here either. Its `callable()` guard is a type-slot probe, not a `__call__` dispatch.
- **Later `partial` invocation remains visible to the explicit-call machinery and currently fails closed** — a `partial` instance and `functools.partial.__call__` both classify `unresolved` under `audit_harness.identity.classify_callable` today, so the deferred invocation of the stored callable surfaces as a counted `calls_unresolved` record rather than as silently accepted execution. This property is the sole reason a deferred-callable *store* is authorized at all: **if a future change makes either of those classify resolved without also resolving the stored target, this entry must be re-reviewed.**

**Predicted effect — predictions only, pending Phase A/B evidence; not achieved results.** Simulated in an isolated, in-memory process against the baseline SHA above, with nothing written to disk and `audit_harness/` untouched:

- `nodes_unresolved`: **22 → 16**
- unresolved node components: **31 → 19**
- `calls_unresolved`: **1092 → 1092** (delta zero — every corresponding explicit call site in `calls_unresolved_detail` targets the *type*, not the authorized slot)

> **SUPERSEDED IN PART BY PHASE B MEASUREMENT (2026-09-03).** The three bullets
> immediately above are preserved verbatim as the historical Phase 0 record of what
> was *predicted*. Phase B measured: `nodes_unresolved` **22 → 16** (prediction
> **confirmed**), `calls_unresolved` **1092 → 1092** (prediction **confirmed**), and
> unresolved node components **31 → 22** — which **supersedes** the `31 → 19`
> predicted above. **`31 → 22` is the governing value.** See the "Task 38.10
> Phase A/B" section below for the measurement, its evidence, and the exact cause
> of the prediction error.

These three figures are **predictions**, not measurements of the committed system. **No `EXACT_IDENTITY_POLICY` entry implementing this authorization exists yet**, `EXACT_IDENTITY_POLICY_VERSION` is unchanged at `2026-08-22.1`, and the current, real, coded blockers remain exactly `nodes_unresolved=22`, `calls_unresolved=1092`, `implicit_dispatch.unresolved_dispatches=6888` (**as of this Phase 0 record, pre-implementation** — the post-implementation measured figures are `16`/`1092`/`6888`, see the "Task 38.10 Phase A/B" section below). **Implementation must occur later, in a separate Phase A commit** carrying the table entries, their individual code-level rationale strings, and their regression tests, followed by a Phase B re-run that measures whatever the real numbers turn out to be. Should Phase B's measured figures differ from the predictions above, the measured figures govern and these predictions are superseded, not defended.

**Explicitly NOT authorized by this decision.** The following remain outside the authorized set and every call or node component reaching them remains `unresolved`. None of these dispositions is softened by this record.

- **Callback-boundary deferred (13)** — each was demonstrated in Phase 0.2 to dispatch caller-controlled protocol code, and authorizing any of them would reduce a *counted* residual by enlarging an *uncounted* one: `builtins.int.__new__` (`__int__`/`__index__`/`__trunc__`), `builtins.range.__new__` (`__index__`), `builtins.bytes.__new__` (`__bytes__`/`__index__`/`__iter__`/`__buffer__`), `builtins.bytearray.__init__` (`__index__`/`__iter__`/`__buffer__`), `builtins.memoryview.__new__` (`__buffer__`), `collections.deque.__init__` (`__iter__`/`__next__`, iterable fully drained during construction), `collections.OrderedDict.__init__` (`keys()` then per-key `__getitem__`, or `__iter__`/`__next__`), `itertools.islice.__new__` (`iter(iterable)` at construction, plus `__index__` on start/stop), `builtins.map.__new__` (`iter(iterable)` at construction; the supplied `func` is not invoked until iteration), `builtins.SyntaxError.__init__` (the two-argument form iterates its second argument), `_thread._ExceptHookArgs.__new__` (structseq construction consumes its argument as a sequence, running user code before the arity check), and both `weakref.ReferenceType.__new__` and `weakref.ReferenceType.__init__` (an arbitrary, unchecked user callable is stored at construction and invoked later at GC time, with no lexical call site anywhere).
- **Other / deeper-review deferred (2)** — `builtins.OSError.__new__`: no callback was observed, but it performs errno-to-subclass type substitution (`OSError(2, 'm')` returns `FileNotFoundError`), a construction-time behavior the walker's node model does not represent, which is an additional independent reason to defer rather than evidence for authorizing; and `builtins.type.__init__`: dispatch-free in the three-argument form, but it cannot resolve the `builtins.type` node while `builtins.type.__new__` is rejected, so authorizing it would enlarge the accepted boundary for no assurance benefit.
- **Rejected (1)** — `builtins.type.__new__` executes arbitrary user code by construction: metaclass `__new__`/`__init__`, descriptor `__set_name__`, and `__init_subclass__` all fired in Phase 0.2's characterization. It must not enter `EXACT_IDENTITY_POLICY`.
- **Evidence-safe but deliberately NOT authorized (3)** — `builtins.OSError.__init__`, `builtins.bytearray.__new__` and `collections.deque.__new__` each met the strengthened safety predicate with no observed dispatch, and are nonetheless withheld: each one's sibling slot remains deferred, so adding it resolves **no node at all** while still enlarging the accepted assurance boundary. They may be revisited only in a future Phase 0 that also disposes of their sibling slot.
- **Family E remains deferred (3)** — `_io.StringIO.__init__`, `_io.StringIO.__new__`, `_io.TextIOWrapper.__init__`. These are I/O types whose sibling identities appear in `FORBIDDEN_IDENTITIES`; they were deferred in Phase 0.1 and are neither authorized nor rejected here.

**M-8 scope correction — a newly evidenced assurance gap, not a new implementation.** While characterizing the C-callable constructor boundaries above, Phase 0.2 established that **M-8's original eleven-family enumeration is incomplete**: it names eleven implicit-dispatch families (two now mechanized under Task 38.8's Option D, nine unmechanized), and at least five further mechanisms fall outside that taxonomy entirely — they are neither mechanized nor counted among the nine known-open families:

1. **numeric coercion** — `__int__` / `__index__` / `__trunc__`;
2. **bytes conversion** — `__bytes__`;
3. **the Python-overridable buffer protocol on CPython 3.12** — `__buffer__`, made overridable from Python by PEP 688, i.e. a mechanism that post-dates M-8's family list and was not Python-reachable on the versions that list was written against;
4. **C-level duck-typed mapping calls** — for example the `keys()` / `__getitem__` paths a C constructor invokes internally, which produce no `ast.Call` node and are not explicit-call machinery either;
5. **class-creation hooks** — `__set_name__` / `__init_subclass__` / metaclass `__new__` / metaclass `__init__`; note specifically that `__set_name__` is **not** `__get__`/`__set__`/`__delete__` and is therefore *not* covered by Task 38.8's mechanized descriptor family;
6. **weakref callbacks** — deferred, GC-timed invocation of an arbitrary callable, with no lexical call site any static site enumeration could reach.

Stated precisely, so this correction is not over- or under-read: these mechanisms **were discovered while characterizing C-callable constructor boundaries** for this task's own authorization question, not by a systematic taxonomy review; they **demonstrate that M-8's existing eleven-family taxonomy is incomplete**, which is a correction to the *scope* of a known finding rather than a new finding; they **do not imply that a reachable forbidden production path has been found** — no such path is claimed, discovered, or implied by this record; they **change no current harness counter**, this being a documentation-and-governance-only phase that runs nothing and implements nothing; they **must not be silently folded into an allowlist**, in `EXACT_IDENTITY_POLICY` or anywhere else, and this record authorizes no such folding; and **future work must decide how to govern and measure them explicitly**, through the same separately-authorized Phase 0 discipline the nine unmechanized families already require, before any implementation touching them begins.

**M-8 disposition: `Open, narrowed` (as recorded by Task 38.8 Phase C, 2026-08-31) → `Open — taxonomy incomplete; narrowed only within the original 11-family taxonomy`** (`docs/audits/task-38.5-risk-register.md`, updated in the same commit as this paragraph). The three-part distinction this wording exists to make explicit:

- **Task 38.8 genuinely narrowed the known gap** — from eleven completely unmechanized families to two mechanized plus nine unmechanized — **within the original eleven-family taxonomy**. That narrowing is real, evidence-backed, and is not retracted here.
- **Task 38.10 Phase 0.2 subsequently found additional implicit/callback mechanisms *outside* that taxonomy** (the six enumerated above), which the eleven-family figures never measured and therefore never narrowed.
- **Therefore the current overall M-8 finding remains `Open`.** "Narrowed" is now only a *historical, sub-scope* characterization — true of the original eleven families as of Task 38.8 — and is **not** a claim that the full present assurance gap is narrower than when M-8 was first recorded.

Nothing about Task 38.8's own narrowing is weakened or advanced by this correction, and no per-family disposition changes: the two mechanized families keep their real per-site evidence, the nine unmechanized families remain open outright, and the out-of-taxonomy mechanisms are newly recorded as ungoverned and unmeasured. **M-8 is not closed, and this correction moves it further from closure, not nearer.**

**Gate outcome: HOLD, unchanged.** This is a documentation-and-governance-only record. It runs no harness, implements no policy entry, and clears no gate predicate. Layer 1 remains independently sufficient to keep this ADR at HOLD on the real, currently committed figures **as of this Phase 0 record**: `nodes_unresolved=22`, `calls_unresolved=1092`, and `implicit_dispatch.unresolved_dispatches=6888` (post-implementation: `16`/`1092`/`6888` — see the "Task 38.10 Phase A/B" section below). The predicted `22 → 16` above is **not** a claim that `nodes_unresolved` is now 16 *at the time this Phase 0 paragraph was written*; Phase B has since measured exactly that reduction. **H-1 remains `Closed`** (Task 38.9B) and **H-2 remains `Closed`** (Task 38.9A), both unchanged by this task; **zero High findings remain open. M-8 remains `Open` — taxonomy incomplete; narrowed only within the original 11-family taxonomy**, per the scope correction above. **Task 39 remains blocked.** No claim is made that `ADR-032` is ALLOWED, that Task 38.10 is complete, or that Task 39 is unblocked.

This update does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review (still paused and unrecorded), and does not modify `EXACT_IDENTITY_POLICY` — no code implementing any of the nine authorized identities exists yet, and Phase A must not begin on the strength of this record alone. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.10 Phase A/B — implementation, evidence, and Phase C governance reconciliation, 2026-09-03.** The nine exact callable-slot identities authorized above are implemented, independently evidenced, and reconciled here against what Phase 0 predicted. The task's three commits preserve Two-Phase Provenance in Git history itself — **authorization → implementation → evidence**, each the direct parent of the next, which git attests without relying on any claim in this document:

1. **Phase 0 — authorization:** `25dd84c8cc3e82676c4fa7c16ae07bacf1494fc7` (`docs(audit): authorize Task 38.10 exact identities`) — the section above, accepting reviewer Aniket Bhalerao, recorded and pushed **before** any implementing code existed.
2. **Phase A — implementation:** `d7fbd32f24227389b851e1165b30cf455a7a15c6` (`test(audit): implement Task 38.10 exact identities`) — `audit_harness/identity.py` and the new `tests/audit_harness/test_task_38_10_phase_a_policy.py`, and nothing else.
3. **Phase B — permanent evidence:** `f1eb47b088536fd97d1063146fd06e5fd65cd255` (`docs(audit): add Task 38.10 Phase B evidence`) — `docs/audits/task-38.10-result.json` (schema `38.10.1`, SHA-256 `2c7a584e6a7f17241b7405cb6f558c2f633c4c14d2291a91ae930848c1e10531`) and `docs/audits/task-38.10-assurance-report.md` (SHA-256 `9f6f21e7321539ad1c946eeed3bddd6e1a810f95763562127bf32dcdaf3185cf`).

**Policy state, measured mechanically rather than asserted.** Phase B extracted both revisions of `audit_harness/identity.py` with `git show`, parsed them with `ast`, and diffed the `EXACT_IDENTITY_POLICY` key sets directly — independent of the committed test assertions. `EXACT_IDENTITY_POLICY_VERSION` moved `2026-08-22.1` → **`2026-09-02.1`**; the table moved **77 → 86 entries**; **exactly 9 keys added**, set-equal to the nine authorized above; **0 keys removed**; **0 unauthorized additions**; no duplicate keys; no wildcard or pattern key anywhere in the table. No **type-level** identity entered the table, and no deferred, rejected, or deliberately-withheld identity entered it.

**Verification battery, re-run from the pushed implementation SHA:** targeted policy tests **13 passed**; `tests/audit_harness` **204 passed**; full project suite **957 passed**; `ruff check audit_harness tests/audit_harness` **clean**; `mypy` **0 errors** in the two paths Phase A touched. The repository-wide `mypy .` failure (`adapters/binance/adapter.py: Source file found twice under different module names`) was reproduced identically, via `git archive` extraction outside the repository, at both `d7fbd32` and pre-implementation `25dd84c` — it predates this task entirely and is **not** a Task 38.10 regression.

**Fresh structural audit — determinism.** `python -m audit_harness.run_audit` was run **twice**, independently, against `d7fbd32`; both runs exit `1` and the two captures are **byte-for-byte identical** (raw SHA-256 `6f42aba92c80f0b58ed95ed752786e65f322b18da9d03b7cb3a617754222f1fa`; canonical `sort_keys`/`indent=2` SHA-256 `e5476f87077ec3928629c7b1c54ae75e9169597cb63adb9698b3b141c1621b41`). Negative controls were satisfied **7/7** (`self_test_failed=false`) before any other part of the result was trusted.

**Current measured residuals (these supersede every pre-implementation figure recorded above):** `nodes_total=268`, **`nodes_unresolved=16`**; `calls_total=7105`, **`calls_unresolved=1092`**; `implicit_dispatch.dispatch_candidates_total=7012` = `resolved_dispatches` **124** + `unresolved_dispatches` **6888** (a genuine partition); module-state 523 candidates / 0 unexplained; runtime denial `bootstrap_status=SUCCESS`, preflight **25/25/0**, `forbidden_call_observed=None`, `success=true`; `exit_code=1`.

**Node reconciliation, measured from a fresh `run_trace()` at each SHA — no node inferred from any prior report.** Six nodes moved to resolved because *every* one of their unresolved components was authorized: `_thread.RLock`, `builtins.AssertionError`, `builtins.AttributeError`, `builtins.NotImplementedError`, `builtins.OverflowError`, `functools.partial`. Sixteen remain unresolved: `_io.StringIO`, `_io.TextIOWrapper`, `_thread._ExceptHookArgs`, `builtins.OSError`, `builtins.SyntaxError`, `builtins.bytearray`, `builtins.bytes`, `builtins.int`, `builtins.map`, `builtins.memoryview`, `builtins.range`, `builtins.type`, `collections.OrderedDict`, `collections.deque`, `itertools.islice`, `weakref.ReferenceType`. No node became newly unresolved; **6 + 16 = 22** closes exactly against the pre-implementation baseline.

### Prediction reconciliation

| Metric | Phase 0 prediction | Phase B measured | Result |
|---|---|---|---|
| `nodes_unresolved` | 22 → 16 | **22 → 16** | **prediction CONFIRMED** |
| `calls_unresolved` | 1092 → 1092 | **1092 → 1092** | **prediction CONFIRMED** |
| unresolved node components | 31 → 19 | **31 → 22** | **prediction SUPERSEDED** |

`calls_unresolved`'s zero delta is the correct outcome, not a null result: all nine identities are per-**slot** keys, and every corresponding explicit call site targets the **type** object, which is deliberately not authorized. `implicit_dispatch.unresolved_dispatches` is unchanged at `6888` because a slot-identity policy addition cannot alter implicit-dispatch *site enumeration*.

**The `31 → 19` component-count prediction was wrong, and is superseded — recorded here in full rather than quietly corrected.**

```
Phase 0 prediction:  31 -> 19
Phase B measured:    31 -> 22
governing value:     31 -> 22
```

**Exact reason.** The Phase 0.2 simulation that produced `19` was run over the complete **12-identity** `SAFE_FOR_EXACT_POLICY` evidence set. Human authorization then **deliberately narrowed the implementation to 9 identities**, withholding three that met the safety predicate but resolve no node while their sibling slot remains deferred:

- `builtins.OSError.__init__` (sibling `builtins.OSError.__new__` deferred)
- `builtins.bytearray.__new__` (sibling `builtins.bytearray.__init__` is a callback boundary)
- `collections.deque.__new__` (sibling `collections.deque.__init__` is a callback boundary)

The `31 → 19` figure was carried into the Phase 0 record from the 12-key simulation instead of being recomputed against the 9-key authorized set. Because those three were neither authorized nor implemented: **31 − 9 = 22**. Phase B confirmed this one-to-one: the set of components that became resolved is *identical* to the set of nine keys added, with no residue.

**This is not a harness defect.** The harness measured correctly, and two independent runs agree byte-for-byte. The error was arithmetic, in a prediction recorded in this document.

**The corrected measurement does not authorize those three withheld identities.** They remain deliberately unauthorized, for the reason recorded in the Phase 0 section and re-confirmed by the node reconciliation above — each still resolves no node while its sibling slot stays deferred. Nothing here converts any Phase 0.2 deferred, rejected, or withheld identity into accepted risk; every one of them remains absent from `EXACT_IDENTITY_POLICY`, and every call or node component reaching them remains `unresolved`.

**Gate outcome: HOLD, unchanged.** **Task 38.10 materially reduced one coded blocker but did not clear it** — `nodes_unresolved` fell from 22 to 16, a real reduction against the same walked universe, and 16 is still nonzero. All three Layer 1 conditions remain independently nonzero and each alone is sufficient to keep this ADR at HOLD: **`nodes_unresolved=16`**, **`calls_unresolved=1092`**, **`implicit_dispatch.unresolved_dispatches=6888`**. The audit's own exit code is `1`. **H-1 remains `Closed`** (Task 38.9B) and **H-2 remains `Closed`** (Task 38.9A); **zero High findings remain open. M-8 remains `Open — taxonomy incomplete; narrowed only within the original 11-family taxonomy`** — Task 38.10 did not remediate M-8, did not mechanize any protocol family, and did not reduce `unresolved_dispatches` by a single site; the out-of-taxonomy mechanisms its Phase 0.2 characterization surfaced (numeric coercion, `__bytes__`, the PEP 688 Python-overridable buffer protocol, C-level duck-typed mapping calls, class-creation hooks, weakref callbacks) remain ungoverned and unmeasured. **Task 39 remains BLOCKED.** No claim is made that `ADR-032` is ALLOWED, that Task 39 is ready, nearly ready, automatically next, or authorized to begin.

This Phase C reconciliation updates this ADR and `docs/audits/task-38.5-risk-register.md` only. It changes no code, no test, no audit mechanic, and no evidence file; it implements no additional identity; and it does not authorize or reject `builtins.str.join`'s separately pending Phase 0 review, which remains **paused and unrecorded**. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Task 38.11 Phase 0.3 — prospective authorization of the DI Registration Provider-Set Resolution mechanism, 2026-09-03.** Per this ADR's Two-Phase Provenance (a human-reviewer authorization must be recorded here *before* any code implementing it is written, never retroactively), the mechanism named and specified below is authorized, prospectively, ahead of its Phase A implementation. Baseline of record for this authorization: `HEAD = origin/main = 7e694f053882ae33e408dbe3ad207b246606f10c`, working tree carrying only the untracked `uv.lock`. **This phase is documentation-and-governance-only:** it changes no code, no test, no evidence file, no harness counter, no gate predicate, and no `EXACT_IDENTITY_POLICY` entry. **Accepting reviewer:** Aniket Bhalerao — project owner/reviewer.

**Authorized mechanism — "DI Registration Provider-Set Resolution."** At a call whose callee is the attribute `provider` on a receiver statically typed `core.interfaces.Registration` (the single syntactic site `core/container.py:116`, `instance = registration.provider(cast(Resolver, self))`), the walker resolves the call against an enumerated, closed set of possible provider targets, under exactly these ten conditions:

1. **Source of target set.** The target set is sourced **only** from the provider objects belonging to the `discovery_container` registry sweep `audit_harness.trace.run_trace` already performs (`audit_harness/trace.py:2716-2726`) and has already inserted into `providers_seen`. The set is captured by reference from that existing enumeration; no new enumeration is performed and no additional composition is executed.
2. **Semantic target count = 214 registrations** — the number of `Registration` entries in the composed registry after the 25 `COMPONENT_REGISTRARS` registrars run, i.e. the number of provider possibilities `registration.provider` can denote in the audited application.
3. **Walker target count = exactly those same 214 provider objects**, already inserted into `providers_seen` and already walked as `provider:` roots by the existing loop at `audit_harness/trace.py:2767`.
4. **The 542 `providers_seen` objects are NOT the semantic target set.** `542 = 214` canonical `discovery_container` objects `+ 328` fresh closure re-instantiations contributed by Part A's 25 additional compositions (`audit_harness/trace.py:2659`, `:2682`). Every one of those 328 is a fresh closure object for a registration key already among the 214 (measured: the Part A key set is a strict subset of the discovery key set, and 45 registrations are reached by the discovery sweep alone), so they add no additional possible target and must not inflate the reported semantic cardinality.
5. **Never independently reconstruct equivalent providers or an equivalent container.** Constructing a fresh `ServiceContainer` and re-running the registrars to obtain "the same" 214 providers is **forbidden** for this mechanism — see the reconstruction hazard recorded below.
6. **Deduplicate only by `id(_underlying(provider))`.** Deduplication by `__module__`/`__qualname__`, by `__code__`, or by source text is **forbidden**, because it is unsound: `StaticWalker.walk` binds closure cell contents into the resolution environment (`audit_harness/trace.py:2130-2139`), so provider objects sharing a code object resolve differently when their cells differ. Measured on the composed registry: one code object, `ServiceContainer.register_class.<locals>._provider`, is shared by 114 registration-bound provider objects carrying **97 distinct closure signatures** (a different `target` class each); and 24 of the 76 `(module, qualname)` identities map to 2–3 **distinct** code objects (sibling `lambda r: r.resolve(...)` providers defined in the same registrar).
7. **Emit exactly ONE aggregate `CallRecord` per site visit**, tagged `resolution_mechanism="di-registration-provider-set"`. Per-target records are not authorized (they inflate `calls_total` from 7105 to 21849 at 76 targets and to 110436 at 542 targets, while changing `calls_unresolved` by nothing).
8. **The aggregate resolves `project_source_available` if and only if** all of: enumeration ran; the target set is non-empty; the walker target count equals the semantic registry count; every provider has retrievable source; and every provider classifies non-`unresolved`. **Any failure of any conjunct makes the entire site `unresolved`.** An empty set never satisfies this predicate vacuously.
9. **`core.interfaces.Registration.provider` — the descriptor itself — is NOT authorized as `project_source_available`.** Nothing in this authorization permits classifying the slot descriptor on its own account, or standing in a single representative identity for the set; the verdict is licensed solely by the conjunction over the enumerated targets.
10. **Every target remains walked** using the existing `provider:`-root semantics (same `owner_class`, same single-parameter `forced_param_hints = {param: ServiceContainer}`). The existing `(id(_underlying(func)), specialization_key)` visited-set must deduplicate them to **zero additional walks**.

**Authorized rationale contents.** The aggregate record's rationale must report the following, each computed from the live enumerated set at emit time and never written as a literal constant: `semantic_targets`, `walked_objects`, `symbol_identities`, `code_objects`, `dedup=object-identity`, and `all_targets_resolved=<true|false>`. `symbol_identities` and `code_objects` are recorded as diagnostics precisely because they are the two unsound deduplication axes: publishing them alongside `semantic_targets` makes any future regression to symbol- or code-based deduplication visible in the report itself.

**Prospective measurement — LABELED PROSPECTIVE, NOT CURRENT.** Measured with a `/tmp`-only patched copy of the harness, using the exact recommended 214-object set, against the baseline SHA above; no repository file was modified to obtain it:

| Metric | Current (committed) | Prospective |
|---|---|---|
| `calls_total` | 7105 | 7105 |
| `calls_unresolved` | **1092** | **901** |
| `visited_funcs` | 1625 | 1625 |
| `nodes_total` / `nodes_unresolved` | 268 / 16 | 268 / 16 |
| `implicit_dispatch` cand. / unres. / res. | 7012 / 6888 / 124 | 7012 / 6888 / 124 |
| `resolve_events` | 383 | 383 |
| probe records emitted / unresolved | — | 191 / 0 |

**This table is authorization evidence only.** It is a prospective simulation, not a committed audit result. **The current committed audit remains `calls_unresolved=1092` until Phase A is implemented and Phase B independently re-measures it.** No claim is made here that 901 is or will be the post-implementation figure of record; Phase B's own measurement governs, and any divergence must be reconciled in this document rather than quietly corrected.

**Reconstruction hazard — recorded explicitly.** An independently reconstructed 214-provider set (a fresh `ServiceContainer` with the same 25 registrars re-run, yielding semantically identical providers at different object identities) was measured under the same aggregate mechanism and produced:

```
calls_total                            7105 -> 7665
visited_funcs                          1625 -> 1839
implicit_dispatch.unresolved_dispatches 6888 -> 7374
calls_unresolved                       1092 ->  901   (identical to the safe variant)
```

Because the visited-set key is `id`-based, reconstructed objects defeat deduplication and every provider body is re-walked under fresh specializations — **regressing Layer-1 blocker `implicit_dispatch.unresolved_dispatches` by 486 while `calls_unresolved` still displays the desired 901.** The mechanism would appear to succeed while making the gate worse. **Reconstructed provider objects are therefore FORBIDDEN for Task 38.11.**

**A further hazard recorded for the same reason:** the prospective metrics do **not** discriminate between the authorized 214-object set and the unsound 76-identity or 101-code-object sets — all three measure `calls_unresolved` 901 with full neutrality, because every member of each is already an independent `provider:` root. The soundness rule in condition 6 must therefore be enforced by an explicit assertion, and must never be justified by these numbers.

**Negative-control authorization.** Phase A is authorized to add the following, and only the following, controls for this mechanism. Three are harness self-tests (each is a claim about the harness's own soundness, the same class as the existing seven): **(1)** a truncated or incomplete provider set fails closed; **(2)** one `unresolved` member makes the whole site `unresolved`, with no partial credit; **(3)** an empty set, or provenance unavailable, fails closed rather than passing vacuously. Four are ordinary unit tests: **(4)** a duplicated provider causes no double walk and no record inflation; **(5)** provider replacement, registry mutation after capture, and completeness are handled per condition 8; **(6)** a sourceless or non-project member fails closed; **(7)** two objects sharing a `__code__` with divergent closure cells remain **distinct** targets and are both walked.

**Expected negative-control count after implementation: `7/7` → `10/10`.** This change is intentional and is **excluded from the metrics-neutrality claim** above, which covers `calls_total`, `visited_funcs`, `nodes_total`, `nodes_unresolved`, `resolve_events`, and every `implicit_dispatch.*` field only.

**Non-goals — explicitly outside this authorization.** No `EXACT_IDENTITY_POLICY` additions (version remains `2026-09-02.1`, 86 entries); no `builtins.str.join` authorization (its Phase 0 review remains **paused and unrecorded**, neither authorized nor rejected here); no `builtins.mappingproxy.items` authorization; no key specialization (`_specialization_key` is unchanged, and `key` is never added — adding it would amplify the `str.join` and `mappingproxy.items` residuals); no symbol- or code-based deduplication; no M-8 implementation and no protocol family mechanized; no third-party traversal boundary; no bound-builtin identity fix; no Task 39 work; and **no attempt to clear the gate**. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Gate impact: none.** This authorization changes no Layer-1 counter. All three coded blockers remain independently nonzero and each alone is sufficient to keep this ADR at HOLD: **`nodes_unresolved=16`**, **`calls_unresolved=1092`**, **`implicit_dispatch.unresolved_dispatches=6888`**; the audit's own `exit_code` is `1`. **`ADR-032` remains INDETERMINATE / HOLD. Task 39 remains BLOCKED.** **H-1 remains `Closed`** (Task 38.9B) and **H-2 remains `Closed`** (Task 38.9A); **zero High findings remain open. M-8 remains `Open — taxonomy incomplete; narrowed only within the original 11-family taxonomy`**, unchanged and unremediated by this phase.

**M-9 — recorded as a separate open Medium finding, not remediated by Task 38.11.** Phase 0.1/0.2 established that `audit_harness.discovery.discover_register_functions` scans only each top-level package's `__init__.py`, and therefore does not see four DI registrar definitions that exist elsewhere: `database.registration.register_persistence`, `core.logging.register_logging`, `adapters.binance.registry.register_binance_adapter`, and `trading.engine.register_trading_engine`. Current evidence is that all four are test-only or dead for the audited production composition (`register_logging` has zero callers anywhere), so **M-9 does not invalidate Task 38.11's provider-set closure** — the authorized mechanism reads its target set from the composed registry itself, never from discovery output, and a registrar no production path invokes can place no entry in that registry. **M-9 is recorded in `docs/audits/task-38.5-risk-register.md` as Medium, Open. It is NOT remediated by Task 38.11**, `audit_harness/discovery.py` is not modified, and its remediation is explicitly not in Phase A scope.

This Phase 0.3 authorization updates this ADR and `docs/audits/task-38.5-risk-register.md` only. It implements nothing itself; the mechanism, its rationale fields, its self-tests, and its unit tests are Phase A work, committed separately per Two-Phase Provenance, and its measured effect is Phase B work.

**Task 38.11 Phase A/B — implementation, evidence, and Phase C governance reconciliation, 2026-09-05.** The `di-registration-provider-set` mechanism authorized above is implemented, independently evidenced, and reconciled here against what Phase 0.3 predicted. The task's three commits preserve Two-Phase Provenance in Git history itself — **authorization → implementation → evidence**, each the direct parent of the next, which git attests without relying on any claim in this document:

1. **Phase 0.3 — authorization:** `b17f4be8002a25372ffcb44c405c6b7d991ea379` (`docs(audit): authorize Task 38.11 provider-set resolution`) — the section above, accepting reviewer Aniket Bhalerao — project owner/reviewer, recorded and pushed **before** any implementing code existed.
2. **Phase A — implementation:** `86820007cba4160f15f04bf3a4f297c705c5afd7` (`feat(audit): implement Task 38.11 Phase A provider-set tracing`) — `audit_harness/trace.py`, `audit_harness/self_test.py`, `tests/audit_harness/test_negative_controls.py`, and the new `tests/audit_harness/test_task_38_11_phase_a.py`, and nothing else.
3. **Phase B — permanent evidence:** `1680e475c7343d59590230572708174abed296d3` (`Task 38.11: add Phase A assurance evidence`) — `docs/audits/task-38.11-result.json` (evidence schema `38.11.1`, SHA-256 `9322137e0d9d919ec0fb2f5ba995a23605c464f56f4d54f134309f551663c8c1`) and `docs/audits/task-38.11-assurance-report.md` (SHA-256 `6a3097403e37f87f9b8c5a854e316bbe60b1c15e6f0592f9285d9e1379f3b072`).

**Measurement of record.** `uv run python -m audit_harness.run_audit`, run against the pushed Phase A commit `86820007`. The harness report schema is **unchanged at `38.8.0`** — Task 38.11 does not bump it; `38.11.1` is the evidence file's own schema id, a separate namespace. The command was run **three times**, independently; all three captures are **byte-for-byte identical** (raw SHA-256 `30cb0df927406afec109f0bc7d844c2acdd83b2728fb9beac68d365c5d1ac1d8`; canonical `sort_keys`/`indent=2` SHA-256 `afee9fff17de44c96fcc981f818e6e117b6626bf39d42c3c2f6cb81a38146841`). Negative controls were satisfied **10/10** (`self_test_failed=false`) before any other part of the result was trusted. A fourth, earlier capture attempt produced a 0-byte file because the shell's working directory had been left outside the repository; that was **operator error in the evidence-capture procedure, not a harness defect and not a nondeterministic result** — the harness never ran — and it is **not** one of the three valid runs. It is disclosed in full in `docs/audits/task-38.11-result.json` under `determinism.operator_error_disclosure`.

**Current measured residuals (these supersede every pre-implementation figure recorded above):** `nodes_total=268`, **`nodes_unresolved=16`** (unchanged by this task); `calls_total=7105` (unchanged), **`calls_unresolved` 1092 → 901**; identity-resolution buckets `project_source_available` **3363 → 3554**, `exact_identity_policy` **2645** (unchanged), `forbidden` **5** (unchanged), `unresolved` **1092 → 901** — summing to `calls_total` **7105** both before and after; `implicit_dispatch.dispatch_candidates_total=7012` = `resolved_dispatches` **124** + `unresolved_dispatches` **6888**, a genuine partition, **every term unchanged by this task**; module-state 523 candidates / 0 unexplained; runtime denial `bootstrap_status=SUCCESS`, preflight **25/25/0**, `forbidden_call_observed=None`, `success=true`; `exit_code=1`. The baseline figures above (`3363`, `1092`) are read from the committed Task 38.10 Phase B evidence, `docs/audits/task-38.10-result.json`; the post-implementation figures are read from `docs/audits/task-38.11-result.json`.

**Provider-set resolution, measured.** **191** aggregate `CallRecord`s carry `resolution_mechanism='di-registration-provider-set'`: **191 resolved / 0 unresolved**, every one categorised `project_source_available`. The rationale is computed live at emit time and is identical across all 191 records: `semantic_targets=214; walked_objects=214; symbol_identities=76; code_objects=101; dedup=object-identity; all_targets_resolved=true`. Phase 0.3's prospective semantic and walker cardinalities of **214 / 214** are both confirmed exactly.

**The 191 records are walker site visits of ONE source AST location — not 191 unique source locations.** This is mechanically verified, not asserted: an `ast` walk of `ServiceContainer.resolve` contains **exactly one** `Call` node whose callee text is `registration.provider` — `registration.provider(cast(Resolver, self))` at `core/container.py:116`. The 191 records are distributed across **74** distinct site labels, all carrying that same callee text, because under the **existing specialization semantics** the walker re-visits that single site once per enclosing specialization. Condition 7 of the authorization above (*exactly ONE aggregate `CallRecord` per site visit*) is satisfied: one record per visit, never one per target.

**76 symbol identities and 101 code objects are diagnostic unsound axes, not authorization predicates.** Deduplication remains **solely `id(_underlying(provider))`**. `symbol_identities` and `code_objects` are published precisely because they are the two axes that would be *unsound* to deduplicate on — recorded so that any future regression to symbol-based or code-object-based deduplication becomes visible in the report itself. Neither participates in the resolution verdict. `scope_confirmation.symbol_or_code_based_dedup_used=false` in the committed evidence.

**Reconciliation: zero residue.** `1092 − 191 == 901`. The −191 delta reconciles one-to-one against the 191 provider-set aggregate records, every one of which resolved to `project_source_available`; **no call site outside that mechanism changed category in either direction**, and the identity buckets close independently (`3363 + 191 = 3554`, with `exact_identity_policy` and `forbidden` both flat). Recorded residue: **0**.

**`EXACT_IDENTITY_POLICY` unchanged.** Version **`2026-09-02.1`** and **86 entries** both before and after — **0 added, 0 removed**. `audit_harness/identity.py` is not among the four paths Phase A touched. `builtins.str.join`'s Phase 0 review remains **paused and unrecorded** — neither authorized nor rejected by this task. *(The `builtins.str.join` review referenced here was subsequently concluded — **AUTHORIZED, prospectively** — by Task 38.12 Phase 0, 2026-09-05; see that section below.)*

**Negative controls 7/7 → 10/10.** Three new fail-closed controls were added by Phase A, exactly as Phase 0.3 authorized: incomplete provider set, one unresolved set member, and an empty-or-unavailable enumeration. This change is **intentional and pre-authorized, and is excluded from the metrics-neutrality claim** above, which covers `calls_total`, `visited_funcs`, `nodes_total`, `nodes_unresolved`, `resolve_events` and every `implicit_dispatch.*` field only.

**Verification battery, re-run from the pushed implementation SHA:** `tests/audit_harness` **211 passed**; full project suite **964 passed**; targeted Phase A tests **15 passed**; `ruff check audit_harness tests/audit_harness` **clean**; `ruff check` on the four Phase A paths **clean**; `mypy` scoped to those four paths **0 errors**; `git diff --check` clean. The repository-wide `ruff check .` reports **75** findings (`UP042` ×68, `UP046` ×4, `B027` ×2, `E501` ×1, across 55 files); **zero** of them fall in any of the four Phase A paths, and they are **pre-existing, not a Task 38.11 regression**.

### Task 38.11 prediction reconciliation

| Metric | Phase 0.3 prediction | Phase B measured | Result |
|---|---|---|---|
| `calls_total` | 7105 | 7105 | Confirmed |
| `calls_unresolved` | 1092 → 901 | 1092 → 901 | Confirmed |
| `nodes_total` / `nodes_unresolved` | 268 / 16 | 268 / 16 | Confirmed |
| `implicit_dispatch` candidates / unresolved / resolved | 7012 / 6888 / 124 | 7012 / 6888 / 124 | Confirmed |
| Provider-set aggregate records emitted / unresolved | 191 / 0 | 191 / 0 | Confirmed |
| `negative_controls` detected / total | 7/7 → 10/10 | 10 / 10 | Confirmed (intentional; excluded from the metrics-neutrality claim) |
| `visited_funcs` | 1625 → 1625 | **not measured** | **Not evaluable from the canonical report** — see below |
| `resolve_events` | 383 → 383 | 383, non-canonical observation only | **Not canonically confirmed** — see below |

**`visited_funcs` and `resolve_events` are not measured by the canonical schema-`38.8.0` report.** Neither is a field of that report: `visited_funcs` is an attribute of `audit_harness.trace.StaticWalker`, not of `TraceResult`, and was not measured in Phase B at all — its prospective `1625 → 1625` figure is therefore **neither confirmed nor refuted** by this evidence. `resolve_events` likewise is not a report field; the value `383` was obtained by direct, read-only `run_trace()` introspection outside the canonical report path and matches the prospective figure, but that is a **non-canonical observation**. **Neither prediction may be described as canonically confirmed**, and neither is relied upon by anything in this reconciliation.

**Gate impact: none. The gate does not move.** Task 38.11 materially reduced one coded blocker but did not clear it — `calls_unresolved` fell **1092 → 901**, a real reduction against the same walked universe, and 901 is still nonzero. All three Layer-1 conditions remain independently nonzero and **each alone is sufficient** to keep this ADR at HOLD: **`nodes_unresolved=16`**, **`calls_unresolved=901`**, **`implicit_dispatch.unresolved_dispatches=6888`**. The audit's own `exit_code` is **`1`**, which is the expected and correct outcome for a gate that remains at HOLD — **a nonzero exit code is never a successful gate result**. The `PASS` verdict recorded in `docs/audits/task-38.11-result.json` applies **only** to conformance with the authorized Task 38.11 mechanism and its evidence requirements; **it is not a gate verdict**. **`ADR-032` remains INDETERMINATE / HOLD. Task 39 remains BLOCKED.** **H-1 remains `Closed`** (Task 38.9B) and **H-2 remains `Closed`** (Task 38.9A); **zero High findings remain open. M-7 remains `Open, narrowed`** — its recorded residual is updated to the measured `nodes_unresolved=16` / `calls_unresolved=901`, which narrows the finding without closing it. **M-8 remains `Open — taxonomy incomplete; narrowed only within the original 11-family taxonomy`**, entirely unchanged: Task 38.11 mechanized no protocol family and did not reduce `unresolved_dispatches` by a single site. **M-9 remains `Open` and unchanged** — the registrar-discovery scan gap is explicitly **not** remediated here; `audit_harness/discovery.py` was not modified, and the authorized mechanism never reads discovery output, sourcing its target set from the composed registry (measured **214 / 214**) instead.

**Phase C scope.** This Phase C reconciliation updates **`docs/architecture/decisions/ADR-032-structural-audit-gate.md` and `docs/audits/task-38.5-risk-register.md` only.** It implements nothing, runs no harness, changes no counter, adds no `EXACT_IDENTITY_POLICY` entry, opens no new finding, and clears no gate predicate. The two Medium-severity documentation observations raised during Phase C planning are **documentation review findings only** — they are deliberately **not** recorded as new risk-register findings, and no `M-10` or `M-11` exists.

**Task 38.12 Phase 0 — resumption and conclusion of the `builtins.str.join` Phase 0 review, 2026-09-05.** Per this ADR's Two-Phase Provenance (a human-reviewer authorization must be recorded here *before* any code implementing the corresponding table entry is written, never retroactively), this section resumes the Phase 0 review of `builtins.str.join` — opened implicitly by Task 38.7's own residual accounting (`docs/audits/task-38.7-assurance-report.md` §4.2, where it was the largest single named family at 191 raw calls) and thereafter recorded as **paused and unrecorded** in eight separate places in this ADR — and concludes it. Baseline of record: `HEAD = origin/main = 01d6587d7340504e36cacedf0b66a38bd283764b`, working tree carrying only the untracked `uv.lock`. **This phase is documentation-and-governance-only:** it changes no code, no test, no evidence file, no harness counter, no gate predicate, and no `EXACT_IDENTITY_POLICY` entry.

**Evidence.** A fresh, read-only diagnostic audit capture was taken at the baseline SHA (`.venv` interpreter, `python -m audit_harness.run_audit`, stdout redirected to `/tmp/task-38.12-phase0-capture.json` — outside the repository, not committed, per the same capture convention Task 38.11 Phase B used). The capture reproduced the Phase B counters of record exactly — `calls_total=7105`, `calls_unresolved=901`, `nodes_unresolved=16`, `implicit_dispatch.unresolved_dispatches=6888`, `exact_identity_policy_version=2026-09-02.1`, `exit_code=1` — establishing that the evidence below describes the same walked universe the committed Task 38.11 evidence describes. Within it:

1. **Current measured residual: 199 raw unresolved calls** are verdict-pinned to module `builtins`, qualname `str.join` (resolution mechanism `literal-type-inference` on a string-literal receiver in every case; `source_available=false` in every case). This figure supersedes §4.2's 191, which was measured on Task 38.7's earlier, narrower walk. Composition, by owning chain: **191** from the 25 registrar provider closures — every one of them the same single source expression, the `" -> ".join(k.__name__ for k in self._resolving)` circular-dependency error-message construction at `core/container.py:108`, reached through `resolver.resolve()` from each closure; **3** from the `core/logging` chain (`core/logging.py:255`'s `", ".join(sorted(mapping))` in `_coerce_level`'s `ValueError` message, plus stdlib `logging.Formatter.__init__`'s own `','.join` reached via `TextFormatter`/`JsonFormatter`'s `super().__init__()`); **5** from the `config.settings.get_settings` → `dotenv` entrypoint chain (error/diagnostic message assembly inside `python-dotenv` and `re` internals). Every one of the 199 constructs an in-process diagnostic or error string.
2. **Explicitly excluded — different identities, not covered by this review:** the same textual `.join` family in the capture also contains **3** `os.path.join` calls and **1** `bytes.join` (`b''.join`) call. Neither identity is reviewed, authorized, or rejected here; all 4 calls remain `unresolved`.
3. **Identity characterization (CPython 3.12.13, isolated interpreter check):** `str.join` is a C-implemented `method_descriptor` (`inspect.getsource` raises `TypeError` — no retrievable `.py` source exists to walk), reached as the exact slot `builtins.str.join` via `audit_harness.identity.module_and_qualname`'s `__objclass__` fallback path. **Irreducibility:** no general mechanism in `audit_harness/` — source-availability walking, literal/local-variable type inference, or any other Requirement 1–5 technique — can classify a C-boundary `method_descriptor` past `unresolved`; the exact same structural argument under which `builtins.str.strip` and `builtins.list.append` were authorized by Task 38.7 Phase 0.
4. **Operational boundary — verified, not assumed:** a pure in-process string concatenation; returns a new `str`; the receiver is immutable and unmutated; no I/O; deterministic in its inputs. Two sharp edges were checked directly rather than presumed benign: **(a)** `str.join` iterates its argument, so any side effects of the argument's own `__iter__`/`__next__` execute during the call — demonstrated in the isolated check with a side-effecting iterable. This is a property of iteration at the call site, not of `str.join`'s own code, and it is identical in kind to the already-authorized iterable-consuming entries `builtins.sorted`, `builtins.any`, `builtins.sum`, `builtins.tuple`, and `builtins.set`; the 199 real sites all pass literals, comprehensions, or generator expressions over project- or stdlib-controlled data. **(b)** `str.join` performs **no** `__str__`/`__repr__` coercion — a non-`str` element raises `TypeError` (verified) — so it cannot invoke arbitrary `__str__` implementations, a strictly narrower dispatch surface than string formatting.
5. **Real-residual benefit:** at 199 of 901 (≈22%), this is the largest single named family in the current unresolved residual — it passes the exact test `builtins.str.upper` failed when Task 38.7 Phase 0 rejected it for zero real-residual benefit.

**Decision: `builtins.str.join` is AUTHORIZED, prospectively** (module `builtins`, qualname `str.join` — the CPython `method_descriptor` reached via `str.join`, resolved through the `__objclass__` fallback path), for a future `EXACT_IDENTITY_POLICY` entry, on the irreducibility, operational-boundary, and real-residual evidence above. **Accepting reviewer:** Aniket Bhalerao — project owner/reviewer. This authorization precedes, and is separate from, its Phase A implementation: the corresponding `EXACT_IDENTITY_POLICY` table entry, its individual code-level rationale string (which must record caveat (a) above), the policy version bump, and its regression tests are future Phase A work, committed separately per Two-Phase Provenance, and **no such code exists as of this record — Phase A must not begin on the strength of this record alone without its own task start.**

**Projected effect (prediction only, to be reconciled by that future task's own Phase B):** `calls_unresolved` 901 → **702** (−199), with every other counter neutral. This is not a claim about the current state; the counters of record remain exactly as Task 38.11 Phase B measured them.

**Non-goals — explicitly outside this authorization.** No `EXACT_IDENTITY_POLICY` modification here (version remains `2026-09-02.1`, **86** entries, 0 added / 0 removed — verified before and after this edit); no `os.path.join` or `bytes.join` authorization (item 2 above); no `builtins.mappingproxy.items` authorization (its own Phase 0 review has never been opened); no M-8 work and no protocol family mechanized; no M-9 work and no `audit_harness/discovery.py` change; no harness, code, or test change of any kind; no Task 39 work; and **no attempt to clear the gate**.

**Gate outcome: HOLD, unchanged.** This record runs no harness against modified code, implements nothing, and clears no gate predicate. Layer 1 remains independently sufficient to keep this ADR at HOLD on the committed figures of record: `nodes_unresolved=16`, `calls_unresolved=901`, `implicit_dispatch.unresolved_dispatches=6888`; `exit_code=1`. **H-1 and H-2 remain `Closed`; M-7 remains `Open, narrowed`** — unchanged, since nothing is implemented and its residual counters have not moved; **M-8 and M-9 remain `Open`, unchanged. Task 39 remains blocked.** No claim is made that `ADR-032` is ALLOWED, that the structural audit is complete, or that Task 39 is unblocked.


## Alternatives Considered
- **No formal gate — treat the audit as informational only.** Rejected: an audit whose findings carry no consequence is easy to produce and easy to ignore; the entire point of running a structural audit before Task 39 is to make its outcome actionable.
- **Gate on any open finding, regardless of severity.** Rejected: with 9 Low findings already on record (mostly `baseline: unknown` typing gaps and test-coverage notes), gating on every open item would block indefinitely on cosmetic issues unrelated to safety. The severity rubric exists precisely so the gate tracks what actually matters — a real reachable I/O/trading/inference/credential-leak path.
- **Gate on High findings too.** Rejected for this run, but not dismissed as a future policy: the two High findings here are about the accuracy of an architectural *claim*, not about an unsafe path existing. Blocking Task 39 on a documentation-accuracy issue would be disproportionate; the risk register's disposition (`Open`) already ensures they aren't forgotten.
- **Re-run the audit until 0 High findings too, before allowing Task 39.** Rejected: this would make audit accuracy — finding real, pre-existing issues — a reason not to ship the audit, incentivizing under-reporting. The gate is about newly-tolerable risk (Critical), not about the audit finding nothing.
- **Treat "0 Critical findings discovered" as equivalent to "0 Critical findings exist" and leave the gate ALLOWED.** Rejected in this closeout pass: five successive passes each found a real gap the last one's method missed, which is direct evidence against treating the current pass as the last word. Conflating "discovered none" with "none exist" would make the gate's own Critical-only criterion unfalsifiable — it would always read ALLOWED regardless of how much of the codebase the method actually covered, which defeats the purpose of having a gate at all.
- **Re-run yet another ad hoc `/tmp` script (a "v6") to try to close the gap immediately.** Rejected for this pass, per its own explicit instruction: the v1–v5 pattern of one-off scripts each fixing the previous one's blind spot is itself part of the problem M-7 names — another one-off script would likely find one more gap and leave the same open-ended uncertainty, not close it. The gap needs a source-controlled, reusable harness (Task 38.6), not another iteration of the same throwaway-script method.

## Consequences
### Positive
- Task 39's eventual start (once the gate is re-evaluated) will rest on a documented, evidence-based, independently-derived, and honestly-scoped confirmation of Task 38's dry-run boundary, rather than an assumption carried forward from Task 38's own completion report or from an overclaimed "complete" audit.
- Two real architectural-claim inaccuracies (H-1, H-2) are now on record with exact evidence, instead of remaining implicit and un-discoverable until someone happens to read all 24 frameworks' imports by hand.
- Seven Medium trading-safety/I-O/assurance gaps (five from v3, M-6 from v4, M-7 from the closeout pass) plus one new Low (L-10, a real narrow typing gap the Task 38.6 harness itself found) are now an explicit, evidence-backed inventory a future task can triage against, rather than something that task would have to rediscover from scratch.
- The Task 38.6 harness turns M-7's four named methodological gaps from prose descriptions into passing, re-runnable tests (`tests/audit_harness/`) — the next commit that touches any of these areas gets checked automatically, not re-derived by hand in a sixth pass.
- The v4 pass corrected a real completeness gap in v3's own method (15 `Engine` classes it could not see) and closed Area 1's narrow-scope caveat on module-level shared state; the v5 pass then corrected v4's own remaining allowlist-by-text shortcuts. This closeout pass names, rather than lets the reader infer, that the pattern of "each pass finds what the last one missed" is itself the finding (M-7) — which is a more honest position than treating the fifth correction as necessarily the last one needed.
- The gate policy itself (Critical-only, now amended to also HOLD on an unresolved assurance gap) is precedent for future structural audits, so the next one doesn't have to re-litigate what severity — or what degree of unresolved uncertainty — should block what.

### Negative
- **Task 39 remains blocked** — Task 38.6 built, committed, and ran the required harness, but its own real result (`nodes_unresolved=2`, `calls_unresolved=68`) does not clear the Gate Rule's zero-unresolved bar, so the gate stays HOLD rather than moving to ALLOWED. This is a narrower, better-understood blocker than before, not a cleared one.
- The two High findings remain unresolved; a future task must still decide whether to correct the documentation claim, migrate the older frameworks toward DI-only isolation, or accept the difference as intentional and document it as such.
- The seven Medium trading-safety/I-O/assurance gaps and the new Low (L-10) remain unimplemented by design (this audit explicitly forbids implementing anything it finds missing) — whatever eventually follows must not assume they've been silently closed.
- `exchange_adapters`' exclusion from the composition root (H-2) means it continues to be un-covered by the 24/24 preflight check until a follow-up task adds it — independently re-confirmed by the Task 38.6 harness's own discovery step.

## Safety Boundaries
This ADR records a gate decision only — it makes no code change and asserts no new safety boundary itself. The safety boundaries it relies on (no forbidden I/O/trading/inference/credential-leak path reachable from `app/main.py:main`/`app/bootstrap.py:run_dry_run_bootstrap`, *within verified scope*) are the ones independently re-verified in `docs/audits/task-38.5-structural-audit.md` Area 3 (and Area 3b for module-level shared state) — themselves now explicitly framed as a high-confidence bounded trace, not a completeness proof. This ADR does not assert those boundaries hold repo-wide beyond what that trace actually covers.

## Related
- Task: Task 38.5 — Structural Audit and Loophole Review (`docs/prompts/task-38.5.md`, `docs/audits/task-38.5-structural-audit.md`, `docs/audits/task-38.5-risk-register.md`, `docs/audits/task-38.5-test-gaps.md`); v4 final-proof pass re-derived Area 3 and added 3b; v5 patch pass corrected v4's call classification and completed 3b's reassignment scan; honest closeout pass (document-only, no new script) retracted completeness language and moved the gate to HOLD — all 2026-08-20.
- Task: Task 38.6 — Audit-Assurance Harness and HOLD Resolution (`docs/prompts/task-38.6.md`, `audit_harness/`, `tests/audit_harness/`, `docs/audits/task-38.6-result.json`, `docs/audits/task-38.6-assurance-report.md`) — 2026-08-20, same day, harness committed at `8fd66ca`, evidence regenerated against that commit. Built, committed, and ran the harness M-7 required; gate re-evaluated, stays HOLD on the harness's own narrower, fully-categorized residual.
- Task: Task 38.7 — Audit Residual Resolution and Gate Re-evaluation (`docs/prompts/task-38.7.md`, Phase A commit `dcb1ebc6b7fd34e89979fd9adf8ed0d57bc8ac58`, `docs/audits/task-38.7-result.json`, `docs/audits/task-38.7-assurance-report.md`) — 2026-08-22. Two ADR-032 Phase 0-authorized `EXACT_IDENTITY_POLICY` entries (`builtins.str.strip`, `builtins.list.append`); Category A–F mechanism work; `mappingproxy`/`unimportable_nodes` closed; **L-10 closed**. Gate re-evaluated across all three layers — stays HOLD (Layer 1 nonzero; Layer 3's H-2 open). Post-evidence addendum, 2026-08-23: **M-8** recorded (implicit protocol-dispatch discovery gap in `StaticWalker`) — see above; does not change any Layer 1 counter or the gate outcome.
- Task: Task 38.8 — Implicit-Execution Discovery Boundary (`docs/prompts/task-38.8.md`; Phase 0 recorded above, 2026-08-24; Phase A.0 characterization, `tests/audit_harness/fixtures/implicit_dispatch.py`/`test_task_38_8_characterization.py`; Phase A.1 mechanism, committed `b338484f8e6e5cd1af1f69260cdb2b044b0db0c5`; Phase B evidence, committed `fa66cea8282ac56dc742289cd03272b18b69318a`, `docs/audits/task-38.8-result.json` hash `7f5ffdf883339fa3e363db8d4a394ba49bb8b0e2416a4e47d7b9790395a7ae53`, `docs/audits/task-38.8-assurance-report.md`; Phase C disposition update recorded above) — 2026-08-24 through 2026-08-31. Mechanized exactly 2 of 11 implicit-dispatch protocol families (context managers, descriptors); **M-8** moved from `Open` to `Open, narrowed` (`docs/audits/task-38.5-risk-register.md`) — never `Closed`, since neither mechanized family reaches zero unresolved sites on this run and 9 families remain entirely unmechanized. Gate re-evaluated — stays HOLD (Layer 1 nonzero; H-2 open). Does not close H-1/H-2, does not authorize or reject `builtins.str.join`'s paused Phase 0 review, does not modify `EXACT_IDENTITY_POLICY`. **Task 39 remains blocked.**
- Task: Task 38.9A — H-2 Exchange Adapter Composition Remediation (implementation, reviewed and committed `b8ebb23531350d30924e777dfdcbd295311f4f10`; Phase B evidence committed `384b5486e74a463b78be648590069e569becd95d`, `docs/audits/task-38.9a-result.json` hash `a1a44b3b793c873a087db21a1e9e4af4e3be0995ba5350f8923a8b3008c6ebc6`, `docs/audits/task-38.9a-assurance-report.md` hash `0b8a8e83b051fabdde9d43e231c66a50547cbc119e9e8ebe616fcb1162c91424`; Phase C disposition update recorded above) — 2026-08-31. Wires `exchange_adapters` as the 25th `COMPONENT_REGISTRARS` entry; real dry-run composition `25/25`; `discovery.missing_from_component_registrars=[]`; runtime denial `SUCCESS`, `25/25/0`, `forbidden_call_observed=None`, `success=True`. **H-2 closed** (`docs/audits/task-38.5-risk-register.md`) — the only disposition this task changed. Gate re-evaluated — stays HOLD (Layer 1 nonzero: `nodes_unresolved=22`, `calls_unresolved=1092`, `implicit_dispatch.unresolved_dispatches=6888`; H-1 open; M-8 `Open, narrowed`). Does not close H-1 or M-8, does not authorize or reject `builtins.str.join`'s paused Phase 0 review, does not modify `EXACT_IDENTITY_POLICY`. **Task 39 remains blocked.**
- Task: Task 38.9B — H-1 Cross-Framework Import Reconciliation (Phase A implementation, committed `78b5332197e03777b689794bdd9cbbd4b60e6e77`, new regression guard `tests/test_h1_cross_framework_boundary.py`; Phase B evidence committed `c628a9d5473da3377d7679de1a7fcd5ef687648b`, `docs/audits/task-38.9b-result.json` schema `38.9b.1` hash `633ab16e1c85a61c85ca232e53af68c03f1db3aceb087bac53066db5af964b6c`, `docs/audits/task-38.9b-assurance-report.md` hash `31cafda06e43e9c85507554de5c6eee4d0f000f5bacf2d11133b9f926ac25eaa`; Phase C disposition update recorded above) — 2026-08-31. Corrects the false blanket "no framework imports another framework directly" claim to the narrower, evidenced invariant: no framework directly constructs another framework's concrete class; the sole reviewed concrete cross-framework runtime type is `trading.engine.TradingEngine`, used by exactly nine frameworks solely via `resolver.has`/`resolver.resolve`, with zero direct construction and zero direct lifecycle calls. **H-1 closed** (`docs/audits/task-38.5-risk-register.md`) — the only disposition this task changed. Gate re-evaluated — stays HOLD (Layer 1 nonzero: `nodes_unresolved=22`, `calls_unresolved=1092`, `implicit_dispatch.unresolved_dispatches=6888`, unchanged since Task 38.9A — no audit-harness re-run was performed; H-2 `Closed`; M-8 `Open, narrowed`). Does not close M-8, does not authorize or reject `builtins.str.join`'s paused Phase 0 review, does not modify `EXACT_IDENTITY_POLICY`. **Task 39 remains blocked.**
- Task: Task 38.10 — Exact-Identity Callback-Boundary Review and Authorization (Phase 0 authorization committed `25dd84c8cc3e82676c4fa7c16ae07bacf1494fc7`; Phase A implementation committed `d7fbd32f24227389b851e1165b30cf455a7a15c6`, `audit_harness/identity.py` + `tests/audit_harness/test_task_38_10_phase_a_policy.py`; Phase B evidence committed `f1eb47b088536fd97d1063146fd06e5fd65cd255`, `docs/audits/task-38.10-result.json` schema `38.10.1` hash `2c7a584e6a7f17241b7405cb6f558c2f633c4c14d2291a91ae930848c1e10531`, `docs/audits/task-38.10-assurance-report.md` hash `9f6f21e7321539ad1c946eeed3bddd6e1a810f95763562127bf32dcdaf3185cf`; Phase C reconciliation recorded above) — 2026-09-02 through 2026-09-03. Added **exactly 9** individually reviewed exact callable-**slot** identities to `EXACT_IDENTITY_POLICY` (`2026-08-22.1` → `2026-09-02.1`, 77 → 86 entries, 0 unauthorized additions, 0 removals) under a callback-boundary predicate strictly stronger than the prior "performs no I/O itself" test. Measured effect: `nodes_unresolved` **22 → 16** (6 nodes resolved, 16 remain), `calls_unresolved` **1092 → 1092**, `implicit_dispatch.unresolved_dispatches` **6888 → 6888**, unresolved node components **31 → 22** (superseding the `31 → 19` Phase 0 prediction — see above). Also recorded M-8's taxonomy scope correction. Gate re-evaluated — **stays HOLD** (Layer 1 nonzero: `nodes_unresolved=16`, `calls_unresolved=1092`, `implicit_dispatch.unresolved_dispatches=6888`); H-1/H-2 remain `Closed`; M-8 remains Open; does not authorize or reject `builtins.str.join`'s paused Phase 0 review; **Task 39 remains blocked.**
- Task: Task 38.11 — DI Registration Provider-Set Resolution (Phase 0.1 closed-world and set-valued dispatch proof; Phase 0.2 provider-set cardinality and scope reconciliation; **Phase 0.3 governance authorization**, 2026-09-03, baseline `7e694f053882ae33e408dbe3ad207b246606f10c`, committed `b17f4be8002a25372ffcb44c405c6b7d991ea379`; **Phase A implementation** committed `86820007cba4160f15f04bf3a4f297c705c5afd7`, `audit_harness/trace.py` + `audit_harness/self_test.py` + `tests/audit_harness/test_negative_controls.py` + `tests/audit_harness/test_task_38_11_phase_a.py`; **Phase B evidence** committed `1680e475c7343d59590230572708174abed296d3`, `docs/audits/task-38.11-result.json` schema `38.11.1` hash `9322137e0d9d919ec0fb2f5ba995a23605c464f56f4d54f134309f551663c8c1`, `docs/audits/task-38.11-assurance-report.md` hash `6a3097403e37f87f9b8c5a854e316bbe60b1c15e6f0592f9285d9e1379f3b072`; **Phase C reconciliation recorded above**) — 2026-09-03 through 2026-09-05. Implemented the `di-registration-provider-set` mechanism for the single `registration.provider(...)` site at `core/container.py:116`: target set sourced only from the existing `discovery_container` sweep already in `providers_seen`, **measured** semantic cardinality **214** and walker cardinality **214**, deduplication solely by `id(_underlying(provider))`, one aggregate `CallRecord` per site visit, conjunctive fail-closed, zero additional walks, zero provider objects reconstructed. **Measured effect** at `86820007`, over three byte-identical audit runs: **191 aggregate provider-set records, 191 resolved / 0 unresolved**, moving `calls_unresolved` **1092 → 901** and `project_source_available` **3363 → 3554**, reconciling with **zero residue**, while `calls_total` (**7105**), `nodes_total`/`nodes_unresolved` (**268**/**16**), `exact_identity_policy` (**2645**), `forbidden` (**5**) and every `implicit_dispatch.*` field (**7012**/**6888**/**124**) are unchanged. The 191 records are **walker site visits of one source AST location under the existing specialization semantics, not 191 unique source locations**; the reported **76** symbol identities and **101** code objects are **diagnostic unsound axes**, never authorization predicates. `visited_funcs` and `resolve_events` are **not fields of the canonical schema-`38.8.0` report** and their predictions are **not** canonically confirmed. Negative controls **7/7 → 10/10**, intentional and excluded from the metrics-neutrality claim. Records **M-9** (registrar-discovery scan gap) as a separate open Medium finding, **not remediated** here — `audit_harness/discovery.py` untouched. Adds no `EXACT_IDENTITY_POLICY` entry (**`2026-09-02.1`, 86 entries**, unchanged), does not authorize or reject `builtins.str.join`'s paused Phase 0 review. Gate re-evaluated — **stays INDETERMINATE / HOLD** (Layer 1 nonzero: `nodes_unresolved=16`, `calls_unresolved=901`, `implicit_dispatch.unresolved_dispatches=6888`; `exit_code=1`); **M-7** residual updated to those measured figures, disposition **`Open, narrowed`** unchanged; **M-8** and **M-9** unchanged; **Task 39 remains blocked.**
- Task: Task 38.12 — `builtins.str.join` Phase 0 Review Resumption and Conclusion (Phase 0 authorization recorded in this ADR, 2026-09-05 — documentation-and-governance-only; **AUTHORIZED, prospectively**; Phase A not begun, `EXACT_IDENTITY_POLICY` unchanged at `2026-09-02.1` / 86 entries)
- Prior task audited: Task 38 — Application Bootstrap & Dry-Run Runtime Composition (`397a706` / `v4.12-application-bootstrap`, `docs/reviews/task-38-review.md`, `docs/architecture/decisions/ADR-031-application-bootstrap.md`)
- Remaining follow-up: a future task should drive `audit_harness/`'s Layer 1 residuals — `nodes_unresolved=16` (reduced from 22 by Task 38.10, not cleared, and unchanged by Task 38.11), `calls_unresolved=901` (reduced from 1092 by Task 38.11, not cleared) — to zero (or record further individually-authorized Phase 0 `EXACT_IDENTITY_POLICY` entries for identities found genuinely irreducible — see `docs/audits/task-38.7-assurance-report.md` §4.1/§4.2 for the residual composition) and re-run the gate; separately, the still-open, separately-governed M-8 implicit-dispatch work should continue — driving `implicit_dispatch.unresolved_dispatches` (currently `6888`) toward zero within the 2 already-mechanized families (context managers, descriptors), or completing a future, separately authorized Phase 0 decision for any of the 9 still-unmechanized families, **and, separately, deciding how to govern and measure the implicit/callback mechanisms Task 38.10 Phase 0.2 found outside the original 11-family taxonomy altogether** (numeric coercion, `__bytes__`, the PEP 688 `__buffer__` protocol, C-level duck-typed mapping calls, class-creation hooks, weakref callbacks) — per M-8's own disposition text in `docs/audits/task-38.5-risk-register.md`. Both High findings (H-1, H-2) are now Closed; neither is remaining remediation work. The `builtins.str.join` Phase 0 review, formerly paused, is now **concluded — AUTHORIZED, prospectively — by Task 38.12 Phase 0** (2026-09-05, above); its Phase A implementation (the policy entry, version bump, and regression tests, projected `calls_unresolved` 901 → 702) is future, separately started work and does not exist yet.
- Gate outcome: **INDETERMINATE / HOLD** — Task 39 must not begin. Current coded blockers, measured at implementation commit `86820007` by Task 38.11 Phase B and recorded in evidence commit `1680e475` (`docs/audits/task-38.11-result.json`, hash `9322137e0d9d919ec0fb2f5ba995a23605c464f56f4d54f134309f551663c8c1`; `docs/audits/task-38.11-assurance-report.md`, hash `6a3097403e37f87f9b8c5a854e316bbe60b1c15e6f0592f9285d9e1379f3b072`): **`nodes_unresolved=16`**, **`calls_unresolved=901`**, **`implicit_dispatch.unresolved_dispatches=6888`** — each independently nonzero, each alone sufficient to keep the gate at HOLD; the audit's own `exit_code` is **`1`**, which is the expected outcome for a gate at HOLD and is never a successful gate result. Task 38.10 materially reduced the first (22 → 16) and Task 38.11 the second (1092 → 901); neither is cleared. This ADR's own lifecycle **Status remains `Accepted`** — that records the gate *policy* as adopted; the *operational gate outcome* below is a separate thing and remains INDETERMINATE / HOLD. 0 Critical findings discovered within verified scope (not the same as 0 Critical findings existing repo-wide); of the 2 High findings originally recorded, **0 remain open** (**H-1 closed by Task 38.9B**, above; **H-2 closed by Task 38.9A**, above); **9 Medium findings open** (**M-1** through **M-5**, unchanged by Task 38.7; **M-6**, unchanged; **M-7**, whose recorded disposition remains **"Open, narrowed"** — the four named methodological gaps are closed as detection capabilities, but the finding itself does not move to Closed until a future pass drives the harness's own residual to zero or each remaining category is individually accepted with recorded justification, per the risk register's own disposition text; Task 38.7 did not change M-7's disposition; **M-8**, added by Task 38.7's post-evidence correction — `StaticWalker`'s implicit protocol-dispatch discovery gap, whose current disposition is `Open — taxonomy incomplete; narrowed only within the original 11-family taxonomy`: Task 38.8 Phase C's `Open, narrowed` remains true of the original eleven families it measured, and Task 38.10 Phase 0.3 records that further mechanisms lie outside that taxonomy entirely, so the present overall gap is not narrower than first recorded, and Task 38.11 changed it in no way; **M-9**, added by Task 38.11 Phase 0.3 — `audit_harness.discovery.discover_register_functions` scans only each top-level package's `__init__.py`, so four DI registrar definitions are invisible to the harness's own completeness check; disposition `Open`, **explicitly not remediated** by Task 38.11, which never reads discovery output) — Task 38.7 closed no pre-existing Medium finding and added one new one, and Task 38.11 added one new one (M-9) while closing none; **L-10 closed** (Task 38.7, the only disposition this task changed besides adding M-8); all tracked in `docs/audits/task-38.5-risk-register.md`.
