# Task 38.5 — Test Gaps

Concrete, actionable enumeration from structural-audit Area 6. Each row names a specific missing test — not a general suggestion — so it can be picked up directly as a future task. None of these was written by this audit; findings only.

## Concurrency

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 1 | `core.container.ServiceContainer`'s own `RLock` — shared by all 24 wired frameworks' resolution/registration path — has no test exercising real concurrent `register`/`resolve` calls from multiple threads. Kept as its own gap, separate from the 21 framework registries below, since it is not itself a "Registry" and is not counted in the 21/1 figure. | `tests/unit/test_container.py` | Repo-wide grep for `threading.Thread(target` under `tests/`: only `tests/test_workflows.py:467`. |
| 2 | Precisely: 22 packages contain a `registry.py`; 21 of them (`agents, backtesting, dashboard, learning, memory, model_gateway, monitoring, notification, optimization, paper_trading, performance, portfolio, positions, reporting, scheduler, storage, strategies, trades, workers, workflows, exchange_adapters`) explicitly claim "thread-safe" in their own docstring (`core/registry.py` is the one registry that makes no such claim). Of those 21, **20 lack a real concurrent-execution test** — only `workflows` has one. | Each of the other 20 frameworks' own unit test file | Repo-wide grep across `tests/` for `threading.Thread(`, `ThreadPoolExecutor`, `concurrent.futures`, `asyncio.gather`, `asyncio.TaskGroup`, `create_task`: exactly one real match, `tests/test_workflows.py:467-477`. |
| 3 | No test runs two independent `app.bootstrap.run_dry_run_bootstrap` calls concurrently against two different candidate containers to confirm they don't interfere via any shared module-level state (`app/wiring.py:_DRY_RUN_MARKET_DATA_PROVIDER`, `core.container.get_container`'s cache). | `tests/test_app_flow.py` | No such test found in `tests/test_app_flow.py`. |

## Adversarial

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 4 | No test constructs a `container_factory` that violates its documented "returns a fresh container every call" precondition (e.g. returns the same instance twice) and observes the actual consequence — Task 38's spec explicitly disclaims responsibility for enforcing this, but nothing demonstrates *what happens* when it's violated. | `tests/test_app_flow.py` | `CompliantFactoryFreshnessAndNonRetentionTests` only exercises a *compliant* factory; no noncompliant-factory test exists. |
| 5 | No test replaces one `register_<framework>` entry with a function that behaves correctly on the surface (registers the expected types) but retains a reference to the `container` parameter past its own call, to confirm nothing in `app/bootstrap.py` would notice or care. | `tests/test_app_flow.py` | No such test found. |
| 6 | No test resolves two different frameworks' Manager types from the *same* candidate container and asserts they don't observe each other's state through an accidentally-shared singleton (beyond the existing `container.has(...)` registration-presence check). | `tests/test_app_flow.py::EndToEndTests` | Existing test checks presence only, not cross-framework state isolation. |

## Boundary

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 7 | No test constructs a `ComponentManifest` at exactly `len(wiring.KNOWN_COMPONENT_IDS)` (24) components with a fully-connected dependency chain (deepest possible topological order) to confirm the planner's heap-based ordering has no depth-related edge case. | `tests/test_app_bootstrap.py` | Existing planner tests use 2-3 component manifests only. |
| 8 | No test checks a `Decimal` priority at a practically extreme value (very large, very small, or with many decimal places) for `app/planner.py`'s tie-breaking heap. | `tests/test_app_bootstrap.py` | Existing tests use single-digit string priorities (`"0"`, `"1"`, `"5"`, `"9"`) only. |
| 9 | No test checks `BootstrapContext.requested_at` at the smallest valid non-zero UTC offset Python's `datetime`/`timezone` supports (as opposed to a clearly-non-zero offset like `UTC+05:30`), to confirm the `utcoffset() == timedelta(0)` check has no off-by-one-representable-value gap. | `tests/test_app_bootstrap.py::BootstrapContextTimestampTests` | Existing rejection test uses `UTC+05:30`, a large offset, not a boundary value. |

## Negative

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 10 | No test constructs a `ComponentManifest` whose components are *all* mutually self-referential (every component depends only on itself) to confirm the self-dependency check fires for every entry, not just the first. | `tests/test_app_bootstrap.py::PlannerTests` | Existing self-dependency test uses a single component. |
| 11 | No test constructs a `Settings` object at the exact edge of a validator's accepted range (e.g. `RISK_MAX_LEVERAGE` at exactly `1.0`, the documented minimum) to confirm `build_configuration_view`'s `Decimal` conversion doesn't introduce a boundary artifact. | A `config`-adjacent or `app/wiring.py`-focused test | No such boundary-value test found for `build_configuration_view`. |

## Integration

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 12 | No integration test covers `exchange_adapters` being added to a manually-constructed manifest and resolved through the real `app/bootstrap.py` pipeline (relevant once H-2 in the risk register is addressed). | A future `tests/test_app_flow.py` addition, after H-2 is resolved | `exchange_adapters` is entirely absent from `app/wiring.py` today — no test can exercise it through this pipeline until that changes. |
| 13 | No test exercises `strategies`' `_build_manager` closure's `market_data`/`engine`/optional-reference resolution path (the `resolver.has(X) if ... else None` pattern found repeatedly in Area 1) inside the full 24-framework default-manifest dry run, to explicitly assert each optional reference resolves to `None` (not an exception) when its sibling framework is absent from a *smaller*, non-default manifest. | `tests/test_app_flow.py` | Existing `EndToEndTests` only exercises the full 24-framework default manifest, never a partial one that would exercise the `None`-fallback branch. |
| 14 | No test asserts a dry-run bootstrap performs zero real I/O even if `LoggerFactory` were registered — i.e. nothing pins down that `logger.get_logger()` (present at 46 sites, see risk register M-6) staying unreached is a structural property of the wiring rather than an accident of which registrars happen to be listed. Per M-6's v5 refinement, a default-configured `LoggerFactory` would attach a console handler (not open a file) if reached — a test would need to distinguish "no I/O at all" from "console handler, no file" once `LoggerFactory` is ever wired in. | `tests/test_app_flow.py` | No test registers `LoggerFactory` into a candidate container and asserts the resulting bootstrap either still performs no I/O or is explicitly rejected; today's coverage only shows `LoggerFactory` is *absent*, never that its presence is safe or blocked. |
| 15 | No test asserts that the 15 registered-but-never-resolved `Engine` providers (`_build_engine` closures — see structural audit Area 3) are actually inert during a dry run, e.g. by resolving `DefaultXEngine` directly against a candidate container built by `run_dry_run_bootstrap` and confirming it constructs without I/O rather than merely being unexercised by the default manifest. | `tests/test_app_flow.py` or each framework's own unit suite | Existing tests never resolve an `Engine` type directly; their I/O-safety today rests on the same source-level classification this audit performed, not on a running assertion. |
| 16 | No *persistent* test patches the file/socket/subprocess/thread/process/sleep surface plus every `start`/`invoke`/`compose`/`schedule`/`enqueue`/`submit_order`/`place_order`/`execute_trade`/`predict`/`infer` method any wired class defines, then asserts `run_dry_run_bootstrap` still succeeds 24/24 — this audit's v5 pass proved it once, ad hoc, in `/tmp/t385/v5_runtime_defence.py`, but nothing in the committed suite re-proves it on every future change to `app/wiring.py`'s 24-framework list. | `tests/test_app_flow.py` | No equivalent test exists in `tests/`; the closest, `DefenceInDepthTests`, patches only `socket.socket`/`Thread.start`/`Process.start`, not the broader surface this audit's own check covers. |

## Audit-Assurance Gap Coverage (risk register M-7)

Four rows, one per methodological gap named in M-7. **Task 38.6 built and ran the harness these rows called for** — rows 17-19 are addressed (implemented and tested); row 20 is partially addressed (the allowlist itself is replaced by a versioned, tested exact-identity policy, but the real run still reports a nonzero, fully-categorized `calls_unresolved` count). Per the Gate Rule, the Task 39 gate stays HOLD until that residual reaches zero or is individually accepted with recorded justification — see `docs/audits/task-38.6-assurance-report.md`.

| # | Gap | Where it belongs | Evidence it's missing |
|---|---|---|---|
| 17 | **Addressed by Task 38.6.** `audit_harness/trace.py:StaticWalker.classify_nodes` now walks every discovered node's `__post_init__` the same way `__init__`/`__new__` are walked, and `tests/audit_harness/test_negative_controls.py::test_forbidden_post_init_is_detected` is the covering test — a fixture `__post_init__` calling `open(...)` is asserted classified `forbidden`. | `audit_harness/trace.py` + `tests/audit_harness/test_negative_controls.py` | Was: no test or script enumerated `__post_init__` as its own call-graph category. Now: implemented and tested; see `docs/audits/task-38.6-assurance-report.md` §1. |
| 18 | **Addressed by Task 38.6.** `audit_harness/runtime_denial.py` patches `os.open`/`os.fdopen`/`posix.read`/`posix.write` directly (not just the higher-level `os.makedirs`/`remove`/`system`), and separately reports DB/Redis client presence (`find_db_redis_exchange_clients`) — explicitly empty for this project's actual dependency set, not silently omitted. `tests/audit_harness/test_negative_controls.py::test_direct_os_open_call_is_classified_forbidden` and `::test_connecting_client_is_intercepted_before_any_real_connection` cover both halves. | `audit_harness/runtime_denial.py` + `tests/audit_harness/test_negative_controls.py` | Was: neither the existing test nor the v5 check patched a file-descriptor primitive or a DB/Redis entrypoint. Now: implemented and tested; see `docs/audits/task-38.6-assurance-report.md` §6. |
| 19 | **Addressed by Task 38.6.** `audit_harness/module_state.py` now detects a plain (non-nested) function mutating a module-level global via method call, subscript assignment, or augmented assignment — distinct from the closure-captured-mutable scan. `tests/audit_harness/test_negative_controls.py::test_module_global_mutation_from_plain_function_is_detected` covers it, and the real audit run found 0 unexplained instances across the full 30-package scope. | `audit_harness/module_state.py` + `tests/audit_harness/test_negative_controls.py` | Was: no AST scan or test checked this shape. Now: implemented and tested; see `docs/audits/task-38.6-assurance-report.md` §5. |
| 20 | **Partially addressed by Task 38.6.** `audit_harness/identity.py`'s versioned exact-identity policy (`2026-08-20.1`) replaces the old hand-maintained text/name allowlist for the calls it does resolve — every accepted no-source identity is an individually-listed, exact module+qualname entry with its own rationale, never a name pattern. What remains open: the harness's own real run still reports `calls_unresolved=68` (fully categorized, no forbidden identity established among them, but unresolved remains safety-unknown, not proven benign — see `docs/audits/task-38.6-assurance-report.md` §4.2) rather than zero, so this gap is narrowed to a specific, bounded residual, not fully closed. | `audit_harness/identity.py` | Was: `SAFE_PURE_TEXT`/`CONTAINER_MECHANISM_TEXT` were hand-maintained, `/tmp`-only sets. Now: a committed, versioned, tested policy table — but the resolved-vs-unresolved split it produces is not yet 100% resolved. |
